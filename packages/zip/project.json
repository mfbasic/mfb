{
  "name": "zip",
  "version": "0.1.0",
  "mfb": "1.0",
  "kind": "package",
  "description": "Reads and writes ZIP archives, including ZIP64. An archive is opened either from an open fs::File or from a List OF Byte, through the same `open` call, and both forms produce the same entries with the same contents. Opening a file-backed archive reads only the central directory, and reading one entry holds only that entry — an archive larger than memory is still readable one entry at a time.\n\nStored (method 0) and deflated (method 8) entries are supported; every entry read is checked against the CRC-32 recorded for it, and an entry whose bytes do not match is refused rather than returned. Encrypted archives, multi-disk archives and any other compression method are refused with errorCode::ErrUnsupported rather than guessed at. Entry names are decoded as UTF-8 when the archive says so and as CP437 otherwise, which is what the format specifies.",
  "sources": [
    {
      "root": "src",
      "role": "package",
      "include": ["**/*.mfb"]
    }
  ]
}
