IMPORT fs

FUNC main AS Integer
  LET missing AS String = fs::pathNormalize()
  LET wrongType AS String = fs::pathNormalize(1)
  LET tooMany AS String = fs::pathNormalize("a", "b")
  RETURN 0
END FUNC
