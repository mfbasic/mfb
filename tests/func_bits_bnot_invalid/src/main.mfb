IMPORT bits

FUNC main AS Integer
  LET a AS Integer = bits::bnot()
  LET b AS Integer = bits::bnot(1, 2)
  LET c AS Integer = bits::bnot(1.5)
  LET d AS Integer = bits::bnot("x")
  RETURN 0
END FUNC
