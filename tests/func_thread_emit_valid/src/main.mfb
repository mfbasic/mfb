IMPORT thread

ISOLATED FUNC worker(t AS ThreadWorker OF String TO Integer, seed AS String) AS Integer
  thread::send(t, seed)
  thread::send(t, "immediate", 0)
  thread::send(t, "timed", 1)
  RETURN len(seed)
END FUNC

FUNC main AS Integer
  RETURN 0
END FUNC
