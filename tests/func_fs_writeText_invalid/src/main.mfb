IMPORT fs

FUNC main AS Integer
  LET missing AS Nothing = fs::writeText()
  LET wrongPath AS Nothing = fs::writeText(1, "x")
  LET wrongValue AS Nothing = fs::writeText("a", 1)
  LET tooMany AS Nothing = fs::writeText("a", "b", "c")
  RETURN 0
END FUNC
