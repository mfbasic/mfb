' Consumer for the PKG-05 security fixture: it imports the malicious
' `sec_count` package and calls an export. The build must fail during package
' decode/verification rather than producing an executable.
IMPORT io
IMPORT sec_count AS sec_count

FUNC main AS Integer
  io::print(toString(sec_count::run()))
  RETURN 0
END FUNC
