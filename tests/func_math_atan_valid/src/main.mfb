IMPORT io
IMPORT math

FUNC main AS Integer
  LET p AS Byte = 2
  LET fixedZero AS Fixed = toFixed("0.00")
  io::print(toString(math::atan(0.0), p))
  io::print(toString(math::atan(1.0), p))
  io::print(toString(math::atan(fixedZero), p))
  RETURN 0
END FUNC
