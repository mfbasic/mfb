IMPORT collections
FUNC same(value AS Integer) AS Integer
  RETURN value
END FUNC

FUNC main AS Integer
  LET numbers AS List OF Integer = [1, 2, 3]
  LET kept AS List OF Integer = collections::filter(numbers, same)
  RETURN len(kept)
END FUNC
