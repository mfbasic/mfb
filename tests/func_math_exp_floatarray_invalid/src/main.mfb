IMPORT math

' exp array overload is Float-only; an Integer list has no exp overload.
FUNC main AS Integer
  LET xs AS List OF Integer = [1, 2, 3]
  LET ys AS List OF Integer = math::exp(xs)
  RETURN 0
END FUNC
