IMPORT math
' sin array overload is Float-only; an Integer list has no sin overload.
FUNC main AS Integer
  LET xs AS List OF Integer = [1, 2, 3]
  LET ys AS List OF Integer = math::sin(xs)
  RETURN 0
END FUNC
