IMPORT collections
FUNC main AS Integer
  LET numbers AS List OF Integer = [1, 2, 3]
  LET value AS Integer = collections::getOr(numbers, 1, "missing")
  LET badName AS Integer = collections::getOr(value := numbers, index := 1, fallbackValue := 0)
  RETURN value
END FUNC
