IMPORT fs

FUNC main AS Integer
  LET missing AS Nothing = fs::deleteDirectory()
  LET wrongType AS Nothing = fs::deleteDirectory(1)
  LET tooMany AS Nothing = fs::deleteDirectory("a", "b")
  RETURN 0
END FUNC
