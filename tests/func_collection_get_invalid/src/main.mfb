IMPORT collections
FUNC main AS Integer
  LET numbers AS List OF Integer = [1, 2, 3]
  ' collections::get with a non-Integer index is a type error.
  LET value AS Integer = collections::get(numbers, "1")
  RETURN value
END FUNC
