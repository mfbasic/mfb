IMPORT bits

FUNC main AS Integer
  LET a AS Integer = bits::popCount()
  LET b AS Integer = bits::popCount(1, 2)
  LET c AS Integer = bits::popCount(1.5)
  LET d AS Integer = bits::popCount("x")
  RETURN 0
END FUNC
