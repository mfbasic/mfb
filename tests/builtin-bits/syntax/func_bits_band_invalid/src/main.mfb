IMPORT bits

FUNC main AS Integer
  LET a AS Integer = bits::band(1)
  LET b AS Integer = bits::band(1, 2, 3)
  LET c AS Integer = bits::band(1.5, 2)
  LET d AS Integer = bits::band("x", 2)
  RETURN 0
END FUNC
