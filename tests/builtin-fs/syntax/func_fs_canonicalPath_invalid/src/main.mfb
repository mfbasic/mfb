IMPORT fs

FUNC main AS Integer
  LET missing AS String = fs::canonicalPath()
  LET wrongType AS String = fs::canonicalPath(1)
  LET tooMany AS String = fs::canonicalPath("a", "b")
  RETURN 0
END FUNC
