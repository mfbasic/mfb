IMPORT fs

FUNC main AS Integer
  LET missing AS Nothing = fs::writeAll()
  LET wrongFile AS Nothing = fs::writeAll("x", "text")
  LET wrongValue AS Nothing = fs::writeAll(fs::openFile("x"), 1)
  LET tooMany AS Nothing = fs::writeAll(fs::openFile("x"), "text", "x")
  RETURN 0
END FUNC
