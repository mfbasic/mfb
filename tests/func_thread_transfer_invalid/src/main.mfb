IMPORT thread
IMPORT thread_workers

FUNC main AS Integer
  LET t AS Thread OF String TO Integer = thread::start(thread_workers::echoText, "a", 1, 1)
  thread::transfer(t, "x")
  LET bad AS String = thread::accept(t)
  RETURN 0
END FUNC
