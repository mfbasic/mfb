IMPORT io
IMPORT math

' sqrt over a List OF Fixed raises ErrInvalidArgument when any lane is negative.
FUNC main AS Integer
  LET xs AS List OF Fixed = [toFixed("4.0"), toFixed("-1.0"), toFixed("9.0")]
  LET sq AS List OF Fixed = math::sqrt(xs)
  io::print(toString(len(sq)))
  RETURN 0
END FUNC
