IMPORT fs

FUNC main AS Integer
  RES missing AS File = fs::openFile()
  RES wrongPath AS File = fs::openFile(1)
  RES wrongMode AS File = fs::openFile("a", 1)
  RES tooMany AS File = fs::openFile("a", "read", "x")
  RES badName AS File = fs::openFile(file := "a")
  RETURN 0
END FUNC
