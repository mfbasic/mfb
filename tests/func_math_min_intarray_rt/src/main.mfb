IMPORT io
IMPORT math
FUNC main AS Integer
  LET a AS List OF Integer = [1, 2, 3]
  LET b AS List OF Integer = [4, 5]
  LET m AS List OF Integer = math::min(a, b)
  io::print(toString(len(m)))
  RETURN 0
END FUNC
