' Guard case: the `DO WHILE ... LOOP` form of the post-test-loop string
' accumulation.
IMPORT io

FUNC main AS Integer
  MUT s AS String = "Z"
  MUT n AS Integer = 0
  DO WHILE n < 4
    s = s & "X"
    n = n + 1
  LOOP
  io::print(s)
  RETURN 0
END FUNC
