IMPORT io
IMPORT thread
IMPORT thread_runtime_workers

FUNC main AS Integer
  LET t AS Thread OF Byte TO Byte = thread::start(thread_runtime_workers::returnByte, "seed")
  thread::send(t, toByte(7), 10)
  LET result AS Byte = thread::waitFor(t)
  io::print(toString(result))
  RETURN 0
END FUNC
