{
  "name": "http_process_coexist_rt",
  "version": "0.1.0",
  "mfb": "1.0",
  "kind": "executable",
  "description": "bug-481 (closed by bug-480 Phase 4): `IMPORT http` and `IMPORT process` must coexist. `http::Stream` (a union) and `process::Stream` (an enum) both claimed the bare name `Stream` in one flat top-level namespace.",
  "sources": [{ "root": "src", "role": "main", "include": ["**/*.mfb"] }],
  "entry": "main",
  "targets": ["native"]
}
