IMPORT fs

FUNC main AS Integer
  LET file AS File = fs::openFile("tests/ownership-conditional-double-close-invalid/src/main.mfb")
  IF TRUE THEN
    fs::close(file)
  END IF
  fs::close(file)
  RETURN 0
END FUNC
