{
  "name": "native_resource_copy_invalid",
  "version": "0.1.0",
  "mfb": "1.0",
  "kind": "package",
  "description": "A native resource is move-only: copying it then using both is rejected (§3).",
  "sources": [ { "root": "src", "role": "package", "include": ["**/*.mfb"] } ]
}
