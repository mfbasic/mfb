IMPORT fs

FUNC main AS Integer
  RES file AS File = fs::openFile("tests/syntax/resources/ownership-conditional-double-close-invalid/src/main.mfb")
  IF TRUE THEN
    fs::close(file)
  END IF
  fs::close(file)
  RETURN 0
END FUNC
