' os::appResourcePath takes at most one String (`relative`, default "").
IMPORT os

FUNC main() AS Integer
  LET wrongType AS String = os::appResourcePath(1)
  LET wrongArity AS String = os::appResourcePath("a", "b")
  RETURN 0
END FUNC
