' os::appCachePath takes at most one String (`relative`, default "").
IMPORT os

FUNC main() AS Integer
  LET wrongType AS String = os::appCachePath(1)
  LET wrongArity AS String = os::appCachePath("a", "b")
  RETURN 0
END FUNC
