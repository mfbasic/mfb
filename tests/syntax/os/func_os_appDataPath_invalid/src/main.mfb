' os::appDataPath takes at most one String (`relative`, default "").
IMPORT os

FUNC main() AS Integer
  LET wrongType AS String = os::appDataPath(1)
  LET wrongArity AS String = os::appDataPath("a", "b")
  RETURN 0
END FUNC
