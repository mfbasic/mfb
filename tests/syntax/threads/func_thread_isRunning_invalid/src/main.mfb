IMPORT thread

FUNC main AS Integer
  LET missing AS Boolean = thread::isRunning()
  LET wrongType AS Boolean = thread::isRunning(1)
  LET tooMany AS Boolean = thread::isRunning(1, 2)
  RETURN 0
END FUNC
