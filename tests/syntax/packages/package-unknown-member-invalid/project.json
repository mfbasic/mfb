{
  "name": "package_unknown_member_invalid",
  "version": "0.1.0",
  "mfb": "1.0",
  "kind": "executable",
  "description": "bug-480: a `pkg::member` naming something the package does not export must be refused where it is written, naming the package and the member.",
  "sources": [{ "root": "src", "role": "main", "include": ["**/*.mfb"] }],
  "packages": [
    {
      "name": "package_import_as",
      "version": "=0.1.0",
      "source": "file:packages/package_import_as.mfp"
    }
  ],
  "entry": "main",
  "targets": ["native"]
}
