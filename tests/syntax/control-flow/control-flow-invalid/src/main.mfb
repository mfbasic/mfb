FUNC main AS Integer
  FOR bad = "x" TO 3 : NEXT
  FOR zero = 1 TO 3 STEP 0 : NEXT
  WHILE 1 : END WHILE
  DO : LOOP UNTIL 1
  DO WHILE "ready" : LOOP
  IF TRUE THEN IF FALSE THEN RETURN 1 END IF ELSE RETURN 2
IF TRUE THEN RETURN 3 ELSE WHILE FALSE : END WHILE
RETURN 0
END FUNC
