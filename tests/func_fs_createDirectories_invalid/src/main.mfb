IMPORT fs

FUNC main AS Integer
  LET missing AS Nothing = fs::createDirectories()
  LET wrongType AS Nothing = fs::createDirectories(1)
  LET tooMany AS Nothing = fs::createDirectories("a", "b")
  RETURN 0
END FUNC
