NAME
  start - start an imported isolated worker function

SYNOPSIS
  thread::start(f AS ISOLATED FUNC(Thread OF Msg TO Out, In) AS Out, data AS In, inboundLimit AS Integer = 64, outboundLimit AS Integer = 64) AS Thread OF Msg TO Out

DESCRIPTION
  Starts exported isolated function f from an imported package in a fresh
  package instance. The worker receives its Thread handle and data argument.

  inboundLimit and outboundLimit bound the worker inbound and outbound queues.
  Omitting both limits uses 64 for each queue. Omitting outboundLimit uses 64.

  Fails with ErrInvalidArgument (10002) when either queue limit is below 1.
  Current-package functions, lambdas, closures, SUBs, non-isolated functions,
  and entry points without the leading Thread parameter are rejected at compile
  time.

EXAMPLES
  LET t AS Thread OF String TO Integer = thread::start(workers::worker, "input")
  LET small AS Thread OF String TO Integer = thread::start(workers::worker, "input", 1, 1)

FIXME
- Inconsistency 1: `src/man/builtins/thread/start.txt`, `specifications/*`, and the shared native helper describe `thread::start` as a working runtime feature, but `src/arch/aarch64_old/mod.rs` routes `NATIVE_OPCODE_THREAD_START` to `emit_thread_default_result`, which aborts the process instead of starting a thread.

Notes: Either remove/decommission the old AArch64 backend path so it is not part of supported `src/**`, or implement `thread::start` there with the same behavior as the shared helper, including queue-limit validation and real thread creation.

Untested: `thread::start(..., 0, 1)` and `thread::start(..., 1, 0)` returning `ErrInvalidArgument`; negative inbound/outbound limits; wrong limit types; too many arguments; lambdas/closures specifically as entry points; runtime proof that omitted limits become `64`; runtime proof that one omitted outbound limit becomes `64`; fresh package instance isolation across multiple starts; old AArch64 backend runtime behavior.
