NAME
  len - return the length of a string

SYNOPSIS
  len(value AS String) AS Integer

PACKAGE
  general

IMPORTS
  None. general functions are available without an IMPORT statement.

DESCRIPTION
  len returns the number of Unicode scalar values in a String. It does not count
  UTF-8 bytes and it does not count user-perceived grapheme clusters. Use
  strings::byteLen for byte length and strings::graphemes when grapheme clusters
  are needed.

OVERLOADS
  len(value AS String) AS Integer
    Returns the number of Unicode scalar values in value.

ERRORS
  len does not fail for supported argument types. Errors can still propagate from
  evaluating the argument expression before len is called.

TYPE CHECKING
  General len is defined only for String. Collection overloads are documented
  under mfb man collection len.

EXAMPLES
  Count string scalar values:

    LET count AS Integer = len("hello")

SEE ALSO
  collection len
  isEmpty
  isNotEmpty
  strings::byteLen
  strings::graphemes

FIXME
- Inconsistency 1: `src/man/builtins/general/len.txt` documents general `len` as string-only, including “General len is defined only for String,” but `specifications/standard_package.md`, `src/builtins/general.rs`, compiler diagnostics, and `tests/func_general_len_valid` define general `len` for `String`, `List OF T`, and `Map OF K TO V`.

Notes: Update `src/man/builtins/general/len.txt` to document all three overloads, or move the collection overloads out of the General spec/source/test surface. Current implementation and tests favor documenting all overloads on the general page while keeping `collection len` as a cross-reference.

Untested: Invalid coverage does not separately exercise every rejected non-supported type named by the collection manual, such as Boolean, Byte, record, resource, thread, function, or lambda. Valid string scalar cases are only asserted through aggregate exit code, not individual printed counts.
