NAME
  toByte - convert an Integer to Byte

SYNOPSIS
  toByte(value AS Integer) AS Byte

PACKAGE
  general

IMPORTS
  None. general functions are available without an IMPORT statement.

DESCRIPTION
  toByte converts an Integer to Byte. Byte is an unsigned 8-bit integer with
  range 0 through 255.

  Byte values may be converted to Integer with toInt. Use toByte when converting
  an Integer value back to Byte.

OVERLOADS
  toByte(value AS Integer) AS Byte
    Converts value to Byte when it is in range.

ERRORS
  ErrOverflow (10028)
    Returned when value is less than 0 or greater than 255.

  Other errors can propagate from evaluating value before toByte is called.

TYPE CHECKING
  toByte accepts only Integer values.

EXAMPLES
  Convert a literal:

    LET value AS Byte = toByte(65)

  Convert an arithmetic result:

    LET original AS Byte = 10
    LET next AS Byte = toByte(original + 1)

SEE ALSO
  toInt
  toString

FIXME
- Inconsistency 1: `src/arch/aarch64_old/mod.rs` implements `toByte` out-of-range failures with `ERR_INVALID_ARGUMENT` (`10002`) for values below `0` or above `255`, while the manual and specifications require `ErrOverflow` / `10028`.

Notes: Change the old AArch64 `emit_general_to_byte` overflow branches to emit `ERR_OVERFLOW` instead of `ERR_INVALID_ARGUMENT`. The current shared native lowering and existing overflow golden for `toByte(256)` match the manual/spec.

Untested: `toByte(-1)` is not covered; only the high overflow case `toByte(256)` is covered. The old AArch64 backend path’s `toByte` error code behavior does not appear covered by the dedicated function tests.
