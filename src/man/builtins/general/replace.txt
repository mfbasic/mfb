NAME
  replace - replace matching text

SYNOPSIS
  replace(value AS String, old AS String, new AS String) AS String

PACKAGE
  general

IMPORTS
  None. general functions are available without an IMPORT statement.

DESCRIPTION
  replace substitutes all non-overlapping occurrences of old with new.

  If old is an empty String, replace returns value unchanged.

OVERLOADS
  replace(value AS String, old AS String, new AS String) AS String
    Replaces all non-overlapping text occurrences.

ERRORS
  replace does not fail for supported argument types and valid evaluated
  arguments. Errors can still propagate from evaluating arguments before replace
  is called.

TYPE CHECKING
  value, old, and new must be String values.

EXAMPLES
  Replace text:

    LET text AS String = replace("hello", "l", "x")

SEE ALSO
  find
  mid
  len

FIXME
- Inconsistency 1: `src/man/builtins/general/replace.txt` says `replace` does not fail for supported argument types and valid evaluated arguments, but `src/target/shared/code/builder_strings.rs` allocates a new result string for matching replacements and emits an allocation-error return path if allocation fails. `specifications/mfbasic.md` also says arena allocation reports `ErrOutOfMemory` through ordinary language-level `Result` propagation.

Notes: Update the manual `ERRORS` section to document possible allocation failure, likely `ErrOutOfMemory (10010)`, or explicitly standardize allocation failure as an implicit runtime failure omitted from per-function manuals.

Untested: allocation failure; output-growing replacement such as replacing `"a"` with `"aa"`; replacement where `new` contains `old`; whole-string replacement; empty input string; multi-character/multi-scalar `old` and `new`; start/end boundary matches.
