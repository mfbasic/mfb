NAME
  mid - return a substring

SYNOPSIS
  mid(value AS String, start AS Integer, count AS Integer) AS String

PACKAGE
  general

IMPORTS
  None. general functions are available without an IMPORT statement.

DESCRIPTION
  mid returns a slice of value beginning at zero-based start and containing
  count Unicode scalar values. Indexes are not byte offsets and not
  grapheme-cluster indexes.

OVERLOADS
  mid(value AS String, start AS Integer, count AS Integer) AS String
    Returns a substring by zero-based Unicode scalar index.

ERRORS
  10001
    Returned when start or count describes an invalid range.

  Other errors can propagate from evaluating arguments before mid is called.

TYPE CHECKING
  value must be String. start and count must be Integer values.

EXAMPLES
  Slice text:

    LET part AS String = mid("hello", 1, 3)

SEE ALSO
  find
  replace
  len

FIXME
- Inconsistency 1: `src/man/builtins/general/mid.txt` and `specifications/standard_package.md` define string `mid` indexes/counts as Unicode scalar indexes, but `src/arch/aarch64_old/mod.rs` implements `emit_general_mid` using string byte length, byte offsets, and byte copy count.

Notes: Resolve by updating the old aarch64 `emit_general_mid` path to locate start/end by UTF-8 scalar boundaries like the shared native lowering and `src/unicode_backend.rs`, while preserving `10001` for negative or out-of-range start/count.

Untested: old aarch64 runtime execution for non-ASCII `mid`; negative `count`; `start == len(value)` with positive `count`; empty-string valid/error boundaries; integer overflow in `start + count`.
