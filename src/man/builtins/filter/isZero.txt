NAME
  isZero - test whether a number is zero

SYNOPSIS
  isZero(value AS Integer) AS Boolean
  isZero(value AS Float) AS Boolean
  isZero(value AS Fixed) AS Boolean

PACKAGE
  filter

IMPORTS
  None. filter functions are available without an IMPORT statement.

DESCRIPTION
  isZero returns TRUE when value is zero. It accepts Integer, Float, and Fixed
  values.

OVERLOADS
  isZero(value AS Integer) AS Boolean
    Returns TRUE when value = 0.

  isZero(value AS Float) AS Boolean
    Returns TRUE when value = 0.0.

  isZero(value AS Fixed) AS Boolean
    Returns TRUE when value = 0.0.

ERRORS
  isZero does not fail for supported evaluated arguments. Errors can still
  propagate from evaluating value before isZero is called.

TYPE CHECKING
  isZero accepts only Integer, Float, and Fixed values.

EXAMPLES
  Test one value:

    LET ok AS Boolean = isZero(0)

  Filter numbers:

    LET values AS List OF Integer = filter([-1, 0, 2], isZero)

SEE ALSO
  isPositive
  isNegative
  filter

FIXME
- Inconsistency 1: `tests/func_filter_isZero_valid/src/main.mfb` appears to test `Fixed` direct calls with `isZero(0.000)` and `isZero(2.500)`, but the golden IR resolves both as `Float` literals, not `Fixed`. The manual documents a `Fixed` overload, and implementation/spec support it, but direct zero and positive `Fixed` calls are not actually covered.

Notes: Update the valid test to call `isZero(zeroFixed)` and `isZero(positiveFixed)` directly, then regenerate goldens. Existing spec, manual, resolver, and native lowering agree on `Integer`, `Float`, and `Fixed`.

Untested: Direct `Fixed` zero and positive direct-call results; invalid rejection for other non-supported types beyond `String`, such as `Boolean`, `Byte`, `List`, or `Map`.
