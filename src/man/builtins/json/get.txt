NAME
  get - read a nested JSON object value by key path

SYNOPSIS
  json::get(value AS Json, path AS List OF String) AS Json

PACKAGE
  json

DESCRIPTION
  json::get walks path through nested Json object members. Each path element is
  matched against an object key. An empty path returns value unchanged.

ERRORS
  ErrNotFound (10004)
    Returned when any path element is absent or when traversal reaches a Json
    value that is not an object before the path is exhausted.

EXAMPLES
  LET city AS Json = json::get(doc, ["user", "address", "city"])

SEE ALSO
  getOr
  parse

FIXME
- Inconsistency 1: `json::get` documents/specifies `ErrNotFound (10004)` when a path component is absent, but `tests/func_json_get_valid` and `tests/func_json_get_invalid` do not exercise that runtime failure.
- Inconsistency 2: `json::get` documents/specifies `ErrNotFound (10004)` when traversal reaches a non-object before the path is exhausted, but no focused test covers that runtime failure.
- Inconsistency 3: The invalid test does not cover the documented second parameter type. It covers 0 args, 3 args, and wrong `value` type, but not wrong `path` type.

Notes: Manual, specifications, and source agree on signature, package, return type, empty-path behavior, object traversal, and `ErrNotFound (10004)` semantics. Resolve by expanding `tests/func_json_get_invalid` for wrong path type and adding runtime validation for absent paths and non-object traversal.

Untested: wrong path argument type; missing top-level key; missing nested key; path continuing through `Null`, `Bool`, `Num`, `Str`, or `Arr`; returned terminal `Arr`/`Num` values.
