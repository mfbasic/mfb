NAME
  len - return the length of a list or map

SYNOPSIS
  len OF T(value AS List OF T) AS Integer
  len OF K, V(value AS Map OF K TO V) AS Integer

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  len returns the size of a collection.

  For List values, len returns the number of items. For Map values, len returns
  the number of key/value entries.

OVERLOADS
  len OF T(value AS List OF T) AS Integer
    Returns the number of items in value.

  len OF K, V(value AS Map OF K TO V) AS Integer
    Returns the number of entries in value.

ERRORS
  len does not fail for supported argument types. Errors can still propagate from
  evaluating the argument expression before len is called.

TYPE CHECKING
  Collection len is defined only for List OF T and Map OF K TO V. Calling this
  overload on a number, Boolean, Byte, record, union, resource, thread,
  function, or lambda is a compile-time type error.

EXAMPLES
  Count list items:

    LET numbers AS List OF Integer = [1, 2, 3]
    LET count AS Integer = len(numbers)

  Count map entries:

    LET ages AS Map OF String TO Integer = Map OF String TO Integer { "Ada" := 36 }
    LET count AS Integer = len(ages)

SEE ALSO
  general len
  isEmpty
  isNotEmpty

FIXME
- Inconsistency 1: `src/man/builtins/collection/len.txt` labels the overloads as `PACKAGE collection`, but `specifications/standard_package.md` lists the `List` and `Map` `len` overloads under General, and `src/builtins/general.rs` resolves `len` as a general built-in accepting `String`, `List OF T`, or `Map OF K TO V`.

Notes: Resolve the taxonomy by either documenting these overloads as part of general `len` with a collection-topic cross-reference, or updating the spec/source organization to expose a real collection package surface for `len`. Current source and diagnostics favor general `len` with collection overloads.

Untested: `func_collection_len_invalid` only checks `len(Integer)`. Collection-specific invalid tests do not cover missing/too many arguments, Boolean, Byte, record, resource, thread, function, lambda, or qualified/packaged usage. Valid collection tests cover empty/non-empty `List OF Integer` and `Map OF String TO Integer`, but not other element/key/value types, nested collection values, or error propagation while evaluating the argument.
