NAME
  reduce - fold a list into one accumulated value

SYNOPSIS
  reduce OF T, U(value AS List OF T, initial AS U, f AS FUNC(U, T) AS U) AS U

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  reduce folds value from left to right. The accumulator starts as initial. For
  each item, reduce calls f(accumulator, item) and uses the returned value as the
  next accumulator. The final accumulator is returned.

  If value is empty, reduce returns initial without calling f.

OVERLOADS
  reduce OF T, U(value AS List OF T, initial AS U, f AS FUNC(U, T) AS U) AS U
    Folds items left to right.

ERRORS
  reduce propagates errors raised by f. Errors can also propagate from
  evaluating value, initial, or f before reduce is called.

TYPE CHECKING
  f must accept the accumulator type U followed by the list element type T, and
  must return U.

EXAMPLES
  Sum with a reducer:

    FUNC add(total AS Integer, value AS Integer) AS Integer
      RETURN total + value
    END FUNC

    LET total AS Integer = reduce([1, 2, 3], 0, add)

  Build text:

    FUNC joinWords(text AS String, word AS String) AS String
      IF text = "" THEN RETURN word
      RETURN text & " " & word
    END FUNC

    LET sentence AS String = reduce(["hello", "world"], "", joinWords)

SEE ALSO
  sum
  transform
  filter

FIXME
- Inconsistency 1: `tests/func_collection_reduce_invalid` does not cover the manual/spec type-checking surface for `reduce`; it only checks one reducer mismatch case.
- Inconsistency 2: The manual documents error propagation from `f`, but the focused `reduce` tests do not exercise a reducer that raises/returns an error.
- Inconsistency 3: The manual says empty lists return `initial` without calling `f`; the valid test checks the returned value, but does not prove `f` is not called.

Notes: The manual signature and behavior match `specifications/standard_package.md`, `specifications/mfbasic.md`, and the native lowering in `src/target/shared/code/builder_collection_queries.rs`. Resolve by expanding `tests/func_collection_reduce_valid` and `tests/func_collection_reduce_invalid` rather than changing the manual or implementation.

Untested: wrong argument count; non-list first argument; non-callable reducer; reducer with wrong element parameter type; reducer with wrong accumulator parameter type as a distinct case; reducer wrong return type as a distinct case; valid `U != T` reduction such as `List OF Integer` to `String`; reducer error propagation; empty-list case proving reducer is not invoked.
