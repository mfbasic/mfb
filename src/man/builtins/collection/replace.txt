NAME
  replace - replace matching list items

SYNOPSIS
  replace OF T(value AS List OF T, old AS T, new AS T) AS List OF T

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  replace returns a new list where every item equal to old is replaced with
  new. The original list is not mutated.

OVERLOADS
  replace OF T(value AS List OF T, old AS T, new AS T) AS List OF T
    Replaces every matching list item.

ERRORS
  replace does not fail for supported evaluated arguments. Errors can still
  propagate from evaluating arguments before replace is called.

TYPE CHECKING
  old and new must have the list element type T.

EXAMPLES
  Replace list items:

    LET values AS List OF Integer = replace([1, 2, 1], 1, 9)

SEE ALSO
  find
  mid
  set

FIXME
- Inconsistency 1: `src/man/builtins/collection/replace.txt` omits the spec-required constraint that `T` must be comparable. `specifications/mfbasic.md` and `specifications/standard_package.md` both say collection `replace` requires comparable element types.
- Inconsistency 2: `src/builtins/general.rs` resolves `replace(List OF T, T, T)` by element type equality only and does not enforce comparability, so source behavior can accept non-comparable list element types despite the specs.

Notes: Add `T must be comparable` to the manual page’s type-checking section, then either enforce comparability in collection `replace` resolution/type checking or relax the specifications if non-comparable replacement is intended.

Untested: non-comparable element rejection such as `List OF List`, `List OF Map`, union/function/lambda/thread/resource elements; comparable element coverage beyond `Integer` and `String` such as Boolean, Byte, Float, Fixed, Nothing, enums, and comparable records; empty list replacement; all-items-match replacement; `old == new`; wrong argument count and non-list receiver coverage specifically for collection `replace`.
