NAME
  filter - keep list items that pass a predicate

SYNOPSIS
  filter OF T(value AS List OF T, predicate AS FUNC(T) AS Boolean) AS List OF T

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  filter calls predicate once for each item in value, from left to right, and
  returns a new list containing the items for which predicate returns TRUE. The
  original list is not mutated.

  Built-in predicate helpers such as isEven, isOdd, isPositive, isNegative,
  isZero, isEmpty, and isNotEmpty are intended for direct use with filter when
  their argument type matches the list element type.

OVERLOADS
  filter OF T(value AS List OF T, predicate AS FUNC(T) AS Boolean) AS List OF T
    Keeps items where predicate returns TRUE.

ERRORS
  filter propagates errors raised by predicate. Errors can also propagate from
  evaluating value or predicate before filter is called.

TYPE CHECKING
  predicate must accept one argument of the list element type T and return
  Boolean.

EXAMPLES
  Keep even numbers:

    LET evens AS List OF Integer = filter([1, 2, 3, 4], isEven)

  Keep non-empty strings:

    LET names AS List OF String = filter(["Ada", "", "Grace"], isNotEmpty)

SEE ALSO
  forEach
  transform
  reduce
  isEven

FIXME
- Inconsistency 1: The manual says `filter` propagates errors raised by `predicate`, but the focused tests do not cover a predicate that fails and prove the error/result is propagated at runtime.
- Inconsistency 2: The manual says the original list is not mutated, but the focused valid test does not verify the source list remains unchanged after filtering.

Notes: Add valid runtime coverage for a failing predicate and for source-list preservation after `filter`. The signature and behavior otherwise match `specifications/standard_package.md`, type checking, IR lowering, and native lowering.

Untested: wrong argument count, non-list first argument, predicate parameter type mismatch, non-function predicate value, built-in predicates `isEmpty`/`isNotEmpty` with list/map element types, predicate error propagation, and source-list immutability.
