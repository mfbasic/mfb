NAME
  forEach - run an action for each list item

SYNOPSIS
  forEach OF T(value AS List OF T, action AS FUNC(T) AS Nothing) AS Nothing

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  forEach calls action once for each item in value, from left to right.

  A SUB(T) is accepted for action because SUB success type is Nothing. If action
  fails for any item, the error auto-propagates and iteration stops.

OVERLOADS
  forEach OF T(value AS List OF T, action AS FUNC(T) AS Nothing) AS Nothing
    Runs action for every item.

ERRORS
  forEach propagates errors raised by action. Errors can also propagate from
  evaluating value or action before forEach is called.

TYPE CHECKING
  action must accept one argument of the list element type T and return Nothing.

EXAMPLES
  Print each string:

    forEach(["hello", "world"], io::print)

  Use a local action:

    SUB printNumber(value AS Integer)
      io::print(toString(value))
    END SUB

    forEach([1, 2, 3], printNumber)

SEE ALSO
  transform
  filter
  reduce

FIXME
- Inconsistency 1: `forEach.txt` documents that callback errors auto-propagate and iteration stops, but `tests/func_collection_forEach_valid` only covers successful callbacks. No targeted test proves stop-on-error behavior.
- Inconsistency 2: `forEach.txt` says `action` must accept one `T` and return `Nothing`, but `tests/func_collection_forEach_invalid` only covers a wrong callback parameter type.

Notes: Add targeted `func_collection_forEach_valid` coverage for a callback that fails after earlier items and proves later items are not visited. Add invalid cases for wrong argument count, non-list first argument, non-function second argument, callback arity mismatch, and callback returning a non-`Nothing` value.

Untested: empty list; lambda callback; callback failure propagation/short-circuit; wrong argument count; non-list receiver; non-function action; callback with wrong arity; callback returning non-`Nothing`.
