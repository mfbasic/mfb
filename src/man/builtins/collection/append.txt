NAME
  append - add an item or items to the end of a list

SYNOPSIS
  append OF T(value AS List OF T, item AS T) AS List OF T
  append OF T(value AS List OF T, items AS List OF T) AS List OF T

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  append returns a new list with content added at the end. The original list is
  not mutated.

OVERLOADS
  append OF T(value AS List OF T, item AS T) AS List OF T
    Returns a list with item added after the existing items.

  append OF T(value AS List OF T, items AS List OF T) AS List OF T
    Returns a list with all items added after the existing items.

ERRORS
  append does not fail for supported evaluated arguments. Errors can still
  propagate from evaluating arguments before append is called.

TYPE CHECKING
  The appended item must have the list element type T, or the appended items
  list must have the same List OF T type.

EXAMPLES
  Append one item:

    LET numbers AS List OF Integer = append([1, 2], 3)

  Append many items:

    LET numbers AS List OF Integer = append([1, 2], [3, 4])

SEE ALSO
  prepend
  insert
  set

FIXME
- Inconsistency 1: `specifications/memory_layouts.md` describes `append` as writing into the existing `List` allocation and incrementing `count`, with only single-item behavior. The manual says `append` returns a new list, does not mutate the original, and supports both single-item and list-of-items overloads. `src/target/shared/code/builder_collection_updates.rs` also allocates a new result list and copies prefix/inserted/suffix entries through shared insert lowering.

Notes: Resolve by updating `specifications/memory_layouts.md` to describe immutable/new-list append behavior for both overloads, or explicitly limit the in-place description to a destructive `MUT` assignment-back optimization if that is the intended low-level path.

Untested: wrong argument count; non-list first argument; list-of-items overload with mismatched element type; appending to an empty list; appending an empty list; appending a list to itself; proof that the original `LET` list remains unchanged; focused runtime coverage for `MUT` assignment-back optimization.
