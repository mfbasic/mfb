NAME
  find - locate list items or sublists

SYNOPSIS
  find OF T(value AS List OF T, item AS T, start AS Integer = 0) AS Integer
  find OF T(value AS List OF T, needle AS List OF T, start AS Integer = 0) AS Integer

PACKAGE
  collection

IMPORTS
  None. collection functions are available without an IMPORT statement.

DESCRIPTION
  find returns the zero-based position of the first match at or after start.

  For item searches, find returns the first index whose item equals item. For
  sublist searches, find returns the first index where the contiguous needle
  sublist begins. An empty sublist matches at start when start is in range.

OVERLOADS
  find OF T(value AS List OF T, item AS T, start AS Integer = 0) AS Integer
    Returns the first list index where item appears.

  find OF T(value AS List OF T, needle AS List OF T, start AS Integer = 0) AS Integer
    Returns the first list index where the contiguous needle sublist appears.

ERRORS
  ErrNotFound (10004)
    Returned when no match exists at or after start.

  10001
    Returned when start is less than 0 or greater than len(value).

TYPE CHECKING
  Item search requires item to have the list element type T. Sublist search
  requires needle to have the same List OF T type as value.

EXAMPLES
  Find a list item:

    LET index AS Integer = find([10, 20, 30], 20)

  Find a sublist:

    LET index AS Integer = find([10, 20, 30], [20, 30])

SEE ALSO
  contains
  get
  mid

FIXME
- Inconsistency 1: `src/man/builtins/collection/find.txt` does not document the spec-required `T must be comparable` constraint for both list item and sublist overloads.
- Inconsistency 2: `src/builtins/general.rs` resolves collection `find` by matching `List OF T`/`T` or `List OF T`/`List OF T` only; it does not enforce the `specifications/*` comparable-element requirement.
- Inconsistency 3: `src/arch/aarch64_old/mod.rs` legacy `emit_collection_find` appears to implement only item search, treats `start > len(value)` as not found instead of `10001`, and does not implement the documented sublist overload.

Notes: Update the manual type-checking section to state that `T` must be comparable. Add comparable-type validation to collection `find` resolution/type checking, or relax the specs if non-comparable list search is intended. Either remove/fix the legacy backend path or clearly exclude it from supported runtime behavior.

Untested: non-comparable element rejection; wrong collection `start` type; wrong argument count for collection-specific calls; negative `start`; `start == len(value)` with non-empty item/sublist search; sublist not found; sublist longer than haystack; empty sublist with `start < len(value)` and `start > len(value)`; comparable element coverage beyond `Integer` and `String`.
