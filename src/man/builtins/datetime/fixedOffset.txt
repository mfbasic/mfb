NAME
  fixedOffset - build a Zone with a constant UTC offset

SYNOPSIS
  datetime::fixedOffset(offsetSeconds AS Integer) AS Zone
  datetime::fixedOffset(hours AS Integer, mins AS Integer) AS Zone

PACKAGE
  datetime

IMPORTS
  IMPORT datetime

DESCRIPTION
  datetime::fixedOffset builds a Zone whose offset from UTC is a constant value
  that does not vary with the instant being projected. Unlike datetime::local,
  whose offset is resolved against the host's configured time zone, and unlike
  datetime::utc, which is the canonical zero-offset zone, a fixed-offset Zone
  carries a single signed offset that applies to every Instant projected through
  it. The returned Zone has a zone kind of ZoneKind::Fixed and a label rendered
  in the form +HH:MM or -HH:MM.

  A Zone is the bridge between the absolute UTC timeline (an Instant) and the
  human-readable civil fields of a DateTime. Projecting an Instant through a
  fixed-offset zone with datetime::inZone produces a DateTime whose year, month,
  day, and time fields are shifted from UTC by exactly the offset this function
  encodes: a positive offset places the civil fields ahead of UTC (east of the
  prime meridian), a negative offset places them behind UTC (west).

  The one-argument form takes the offset directly as a raw signed second count.
  The two-argument form takes whole hours and a minute magnitude: mins must be in
  the range 0 .. 59 and contributes its magnitude only, taking the sign of the
  hours argument. Thus datetime::fixedOffset(-5, 30) is -05:30 (five hours and
  thirty minutes behind UTC), and datetime::fixedOffset(5, 30) is +05:30. The
  two-argument form is implemented in terms of the one-argument form by combining
  the hours and minutes into a total second count.

  In both forms the offset magnitude must be strictly under 24 hours (86400
  seconds); an offset of exactly +/-24h or more is rejected. The function is
  pure: it reads no host state and has no side effects.

OVERLOADS
  datetime::fixedOffset(offsetSeconds AS Integer) AS Zone
    Builds a fixed zone whose offset is offsetSeconds, interpreted as a signed
    count of seconds east (positive) or west (negative) of UTC.

  datetime::fixedOffset(hours AS Integer, mins AS Integer) AS Zone
    Builds a fixed zone from whole hours and a 0 .. 59 minute magnitude. The
    minutes take the sign of hours, so the combined offset is
    sign(hours) * (abs(hours) * 3600 + mins * 60) seconds.

PARAMETERS
  offsetSeconds AS Integer
    The signed offset from UTC in seconds. Positive values are east of UTC,
    negative values are west. The magnitude must be strictly less than 86400
    (24 hours).

  hours AS Integer
    The whole-hour component of the offset, signed. Its sign determines the sign
    of the whole resulting offset, including the minutes contribution.

  mins AS Integer
    The minute magnitude of the offset, in the range 0 .. 59. It contributes its
    magnitude only and inherits the sign of hours.

RETURN VALUE
  AS Zone
    A Zone with zone kind ZoneKind::Fixed, the requested constant offset, and a
    label of the form +HH:MM or -HH:MM. For a zero offset the label is +00:00.

ERRORS
  77050002 (ErrInvalidArgument)
  Raised when the resulting offset magnitude is 24 hours (86400 seconds) or more,
  or, in the two-argument form, when mins is outside the range 0 .. 59.

EXAMPLES
  Build a zone five and a half hours behind UTC:

    IMPORT datetime

    LET z AS Zone = datetime::fixedOffset(-5, 30)

  Build the same zone from a raw second count:

    IMPORT datetime

    LET z AS Zone = datetime::fixedOffset(-19800)

  Project the current instant into a fixed +09:00 zone:

    IMPORT datetime

    LET t AS Instant = datetime::now()
    LET local AS DateTime = datetime::inZone(t, datetime::fixedOffset(9, 0))

SEE ALSO
  datetime::utc, datetime::local, datetime::inZone, datetime::offsetAt
