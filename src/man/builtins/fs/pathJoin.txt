NAME
  pathJoin - join path components

SYNOPSIS
  fs::pathJoin(parts AS List OF String) AS String

DESCRIPTION
  Joins path components with the POSIX separator used by the current native
  targets.

EXAMPLES
  LET path AS String = fs::pathJoin(["target", "output.txt"])

FIXME
- Inconsistency 1: `src/man/builtins/fs/pathJoin.txt` says `pathJoin` joins components with the POSIX separator used by current native targets, while `specifications/standard_package.md` says it uses the host platform’s separator and normal separator rules. `src/target/shared/code/builder_fs_paths.rs` hardcodes `"/"` and delegates to `strings.join`, so it does not implement host-specific separators or normal path separator rules.

Notes: Resolve by choosing the intended contract. If `pathJoin` is meant to be POSIX-only string joining on current supported targets, update `specifications/standard_package.md` to match and remove “host platform” / “normal separator rules.” If it is meant to follow host path semantics, update the shared implementation and tests accordingly. Implement real `pathJoin` support in the legacy AArch64 backend or remove/disable that backend path if unsupported.

Untested: Separator-normalization behavior for leading/trailing separators, empty string components, absolute components, repeated slashes, `"."` / `".."` components, backslash or drive-style paths, and execution through the legacy AArch64 backend path.
