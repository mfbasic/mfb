//! `collections::isSubset` is a source-generic member: its MFBASIC body lives in
//! the self-contained `package.mfb` (injected via the manifest path) and its
//! return type comes from generic instantiation. The clean-room registry does
//! not model source-generic members, so this module is intentionally empty.
