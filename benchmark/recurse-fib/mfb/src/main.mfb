IMPORT io

' Naive recursive Fibonacci — exercises pure call/return overhead.
FUNC fib(n AS Integer) AS Integer
  IF n < 2 THEN RETURN n
  RETURN fib(n - 1) + fib(n - 2)
END FUNC

FUNC main AS Integer
  LET result AS Integer = fib(35)
  io::print("fib(35): " & toString(result))
  RETURN 0
END FUNC
