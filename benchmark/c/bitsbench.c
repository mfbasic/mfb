/* GROUP: bits (bits:: package coverage) — one row per op.
 * GENERATED by benchmark/gen_bits.py — do not edit by hand; regenerate:
 *   python3 gen_bits.py c > c/bitsbench.c
 *
 * Each row folds acc ^= OP(x[, k]) over x = 0..N-1 on uint64_t, reported as a
 * signed i64 so the checksum matches mfb / Python. clz(0)/ctz(0) = 64 (mfb's
 * defined result); the 32-bit ops act on the low 32 bits and zero-extend. */
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

#include "bench.h"
#include "bitsbench.h"

static inline uint64_t rotl64(uint64_t v, unsigned s) {
  s &= 63u;
  return s ? (v << s) | (v >> (64 - s)) : v;
}
static inline uint64_t rotr64(uint64_t v, unsigned s) {
  s &= 63u;
  return s ? (v >> s) | (v << (64 - s)) : v;
}
static inline uint32_t rotl32(uint32_t v, unsigned s) {
  s &= 31u;
  return s ? (v << s) | (v >> (32 - s)) : v;
}
static inline uint32_t rotr32(uint32_t v, unsigned s) {
  s &= 31u;
  return s ? (v >> s) | (v << (32 - s)) : v;
}
static inline int clz64(uint64_t v) { return v ? __builtin_clzll(v) : 64; }
static inline int ctz64(uint64_t v) { return v ? __builtin_ctzll(v) : 64; }

static void test_bits_band(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((x & 6148914691236517205ULL));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_band = %lld\n", checksum);
  record("bits", "band", t, RUN);
  free(t);
}

static void test_bits_bor(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((x | 6148914691236517205ULL));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_bor = %lld\n", checksum);
  record("bits", "bor", t, RUN);
  free(t);
}

static void test_bits_bxor(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((x ^ 6148914691236517205ULL));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_bxor = %lld\n", checksum);
  record("bits", "bxor", t, RUN);
  free(t);
}

static void test_bits_bnot(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((~x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_bnot = %lld\n", checksum);
  record("bits", "bnot", t, RUN);
  free(t);
}

static void test_bits_sl(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((x << 3));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_sl = %lld\n", checksum);
  record("bits", "sl", t, RUN);
  free(t);
}

static void test_bits_sr(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((x >> 3));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_sr = %lld\n", checksum);
  record("bits", "sr", t, RUN);
  free(t);
}

static void test_bits_sra(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)((int64_t)x >> 2));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_sra = %lld\n", checksum);
  record("bits", "sra", t, RUN);
  free(t);
}

static void test_bits_rl32(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)rotl32((uint32_t)x, 5));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_rl32 = %lld\n", checksum);
  record("bits", "rl32", t, RUN);
  free(t);
}

static void test_bits_rr32(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)rotr32((uint32_t)x, 9));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_rr32 = %lld\n", checksum);
  record("bits", "rr32", t, RUN);
  free(t);
}

static void test_bits_rl64(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ (rotl64(x, 7));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_rl64 = %lld\n", checksum);
  record("bits", "rl64", t, RUN);
  free(t);
}

static void test_bits_rr64(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ (rotr64(x, 3));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_rr64 = %lld\n", checksum);
  record("bits", "rr64", t, RUN);
  free(t);
}

static void test_bits_clz(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)clz64(x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_clz = %lld\n", checksum);
  record("bits", "clz", t, RUN);
  free(t);
}

static void test_bits_ctz(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)ctz64(x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_ctz = %lld\n", checksum);
  record("bits", "ctz", t, RUN);
  free(t);
}

static void test_bits_popCount(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)__builtin_popcountll(x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_popCount = %lld\n", checksum);
  record("bits", "popCount", t, RUN);
  free(t);
}

static void test_bits_bswap16(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)__builtin_bswap16((uint16_t)x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_bswap16 = %lld\n", checksum);
  record("bits", "bswap16", t, RUN);
  free(t);
}

static void test_bits_bswap32(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ ((uint64_t)__builtin_bswap32((uint32_t)x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_bswap32 = %lld\n", checksum);
  record("bits", "bswap32", t, RUN);
  free(t);
}

static void test_bits_bswap64(void) {
  long long *t = alloc_times();
  long long checksum = 0;
  for (int r = 0; r < RUN; r++) {
    long long t0 = now_ns();
    uint64_t acc = 2166136261ULL;
    for (int i = 0; i < 200000; i++) {
      uint64_t x = (uint64_t)i;
      acc = rotl64(acc, 7) ^ (__builtin_bswap64(x));
    }
    checksum = (long long)acc;
    t[r] = now_ns() - t0;
  }
  fprintf(stderr, "bits_bswap64 = %lld\n", checksum);
  record("bits", "bswap64", t, RUN);
  free(t);
}

void run_bits_group(void) {
  test_bits_band();
  test_bits_bor();
  test_bits_bxor();
  test_bits_bnot();
  test_bits_sl();
  test_bits_sr();
  test_bits_sra();
  test_bits_rl32();
  test_bits_rr32();
  test_bits_rl64();
  test_bits_rr64();
  test_bits_clz();
  test_bits_ctz();
  test_bits_popCount();
  test_bits_bswap16();
  test_bits_bswap32();
  test_bits_bswap64();
}
