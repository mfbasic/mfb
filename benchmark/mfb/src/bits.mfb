' Bits-group benchmark (bits:: package). GENERATED by benchmark/gen_bits.py — do
' not edit by hand; edit the generator and regenerate:
'   python3 gen_bits.py mfb > mfb/src/bits.mfb
'
' One row per bits:: op. Each folds acc = bxor(acc, OP(i[, k])) over i=0..N-1 so
' the checksum is a deterministic 64-bit pattern identical across mfb / C /
' Python. Bits are Integer-only, so there is no Fixed/Dynamic split.
IMPORT io
IMPORT bits
IMPORT collections
IMPORT datetime

FUNC test_bits_band(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::band(i, 6148914691236517205))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_band = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "band", times))
END FUNC

FUNC test_bits_bor(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::bor(i, 6148914691236517205))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_bor = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "bor", times))
END FUNC

FUNC test_bits_bxor(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::bxor(i, 6148914691236517205))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_bxor = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "bxor", times))
END FUNC

FUNC test_bits_bnot(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::bnot(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_bnot = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "bnot", times))
END FUNC

FUNC test_bits_sl(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::sl(i, 3))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_sl = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "sl", times))
END FUNC

FUNC test_bits_sr(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::sr(i, 3))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_sr = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "sr", times))
END FUNC

FUNC test_bits_sra(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::sra(i, 2))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_sra = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "sra", times))
END FUNC

FUNC test_bits_rl32(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::rl32(i, 5))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_rl32 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "rl32", times))
END FUNC

FUNC test_bits_rr32(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::rr32(i, 9))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_rr32 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "rr32", times))
END FUNC

FUNC test_bits_rl64(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::rl64(i, 7))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_rl64 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "rl64", times))
END FUNC

FUNC test_bits_rr64(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::rr64(i, 3))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_rr64 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "rr64", times))
END FUNC

FUNC test_bits_clz(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::clz(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_clz = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "clz", times))
END FUNC

FUNC test_bits_ctz(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::ctz(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_ctz = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "ctz", times))
END FUNC

FUNC test_bits_popCount(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::popCount(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_popCount = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "popCount", times))
END FUNC

FUNC test_bits_bswap16(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::bswap16(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_bswap16 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "bswap16", times))
END FUNC

FUNC test_bits_bswap32(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::bswap32(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_bswap32 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "bswap32", times))
END FUNC

FUNC test_bits_bswap64(run AS Integer, tests AS List OF BenchResult) AS List OF BenchResult
  MUT times AS List OF Integer = []
  MUT checksum AS Integer = 0
  FOR r = 0 TO run - 1
    LET t0 AS Integer = datetime::monotonicNanos()
    MUT acc AS Integer = 2166136261
    FOR i = 0 TO 199999
      acc = bits::bxor(bits::rl64(acc, 7), bits::bswap64(i))
    NEXT
    checksum = acc
    LET t1 AS Integer = datetime::monotonicNanos()
    times = collections::append(times, t1 - t0)
  NEXT
  io::printError("bits_bswap64 = " & toString(checksum))
  RETURN collections::append(tests, makeResult("bits", "bswap64", times))
END FUNC
