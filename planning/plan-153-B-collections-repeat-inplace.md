# plan-153-B: `collections::repeat` — the in-place self-update arm

Last updated: 2026-09-24
Effort: medium (1h–2h)
Depends on: plan-153-A

This sub-plan makes `xs = collections::repeat(xs, v, count[, startAt])` update
`xs` where it stands:

- range writes are O(`count`);
- growing the list costs at most one grow;
- no copy of `xs` is made.

It applies at every site the self-update census covers:
- a local;
- a module-level global;
- inside a `FOR EACH` over `xs`;
- a `MUT` captured by reference;
- a record field;
- a `RES … STATE` field.

It also adds the census row the self-update policy requires. That policy says
a self-update-shaped builtin gets an arm or a proven exemption, never a
promise (`SelfUpdate` in `src/codegen/collection/assign/self_update.rs`).

**Correct behavior.** Two things must hold:

- **Allocation.** Take the `cases.tsv` harness statement
  `x = collections::repeat(x, 7, {M})`. It must satisfy the `arm` bound
  `count(2N) - count(N) < N/8`, and the result must equal plan-153-A's value
  path.
- **Failure atomicity.** A failing call must leave `x` exactly as it was.

References:

- plan-153-A (the value path and the contract).
- `mfb spec memory collections` *Self-updates* and *Capacity Headroom and Growth*.
- `.ai/collections.md` — the in-place arms assume one owner (bug-601), and the
  self-update census rules.

## Prerequisites

| Must be true | Command | Status |
|---|---|---|
| plan-153-A complete | `ls planning/plan-153-A-*` → no match (archived to `planning/completed/`) | NOT MET (2026-09-24) |

If plan-153-A is not complete, this plan cannot start, full stop.

> **NOTE — the Status column is a snapshot; the Command column is the truth.**
> Re-run every command before you continue, and again before you decide to stop.

## 1. Goal

- The `cases.tsv` row for `repeat` passes as `arm`.
- Every field site in `field_expect.tsv` reports the expected result.
- `tests/runtime/rt_inplace_failure_atomic.rs` gets two `repeat` cases, a
  negative count and `startAt > len`, and both leave `x` unchanged.

### Non-goals (explicit constraints)

- **`strings::repeat`'s arm is unchanged.** It keeps its `STRING_GROW_FNS`
  entry and its `StrGrow` route.
- **plan-153-A's value-path output is unchanged**, and so are its goldens.
- **No new growth policy:** use `emit_geometric_step` as it is.

## 2. Current State

- **The dispatcher.** `CodeBuilder::try_inplace_self_update`
  (`src/codegen/collection/assign/self_update.rs`) walks `SELF_UPDATE_ARMS`.
  Adding an arm takes four entries in that file:
  - an `ArmId` variant;
  - a `SELF_UPDATE_ARMS` row (`ArmId`, arm fn, `FieldReach`);
  - an `ArmId::markers` entry;
  - a `SELF_UPDATE_TABLE` row (`function: "collections::repeat"`,
    `kind: SelfUpdate::Arm(&[ArmId::Repeat])`, probes).
- **Arm bodies to copy.** `try_inplace_set_assign` and
  `try_inplace_insert_assign` (`src/codegen/collection/assign/builder_inplace_assign.rs`)
  both begin with `resolve_self_update(site, value, "<name>", <argc>)`
  (`inplace_dest.rs`), which demands an exact argument count.
- **The short-name collision.** `self_update_builtin` resolves both
  `collections.repeat` and `strings.repeat` to `"repeat"`. The string arm
  declines by arity: `STRING_GROW_FNS` has `("repeat", 2..=2, …)` in
  `string_self_update.rs`. The new arm must decline unless the destination is
  a `List` and there are 4 arguments. The `self_update_builtin_names_every_spelling`
  test must list `("collections.repeat", "repeat")`.
- **Ownership lists.** `COLLECTION_SELF_MUTATORS`
  (`src/codegen/resource/cleanup/record_ownership.rs`) lists the qualified
  collection mutators.
- **Grow once for a batch.** `lower_list_bulk_append_in_place`
  (`src/codegen/collection/list/list_mutate.rs`) grows once for a whole batch;
  the extend part of `repeat` copies its shape.
- **Growing a field.** `field_realloc_admitted` (`inplace_dest.rs`) allows a
  growing operation on a field only when that field is the owner's last
  inlined field.
- **The census harness** has three parts:
  - `tests/guards/inplace_self_update_census.rs` matches `mfb man` signatures
    against `tests/runtime/inplace_self_update/cases.tsv`;
  - `tests/runtime/rt_inplace_self_update.rs` runs each row under `--debug`
    and applies the `arm` bound;
  - `field_expect.tsv` is generated by `field_expect_gen.py`.

### Measured populations

| What | Count | Command |
|---|---|---|
| `collections::` rows in cases.tsv today | 27 | `grep -c "^collections::" tests/runtime/inplace_self_update/cases.tsv` → 27 (re-measure at start; plan-153-A must not have changed it) |

### Verified properties

- **The string arm declines 4 arguments.** Its arity range is `2..=2`,
  measured by
  `grep -n '"repeat"' src/codegen/collection/assign/string_self_update.rs`.
  UNVERIFIED: whether the arity check runs before any emission. Phase 1's
  first task confirms it.

## 3. Design Overview

`try_inplace_repeat_assign` runs in this order:

1. `resolve_self_update(site, value, "repeat", 4)`. The destination must be a
   `List`.
2. Evaluate `value`, `count` and `startAt`. Validate `count >= 0` and
   `0 <= startAt <= len(x)`. Raise **before any write**, which is what makes
   the call failure-atomic.
3. If `startAt + count > len(x)`, grow once, to at least `startAt + count`
   slots.
4. Overwrite `[startAt, min(len, startAt + count))` with the same primitive
   `set` uses.
5. Append the tail with the bulk-append shape.

For a fixed-width element, steps 4 and 5 become one store loop.

For field sites, use `FieldReach` like `set`/`insert`. A growing call on a
field that is not the last inlined field falls back to a rebuild, per
`field_realloc_admitted`. Put that site in `field_expect.tsv` as a rebuild,
not an arm.

**Correctness risk.** It concentrates in two places:

- **Aliasing.** `value` may read `x`, as in `x = repeat(x, collections::get(x, 0), 3)`.
  It must be evaluated and owned before the grow frees `x`'s old block. The
  `set` arm solves the same problem by materializing the item first; copy that
  order.
- **The string arm's decline** must stay by arity.

**Gate class:** behavior-preserving at the value level, but allocation counts
change by design. The gates are the census harness and the failure-atomicity
tests. The `.ncode` for programs that don't call `repeat` must not change: a
diff there means the matcher leaked into shared code.

Rejected alternative: **a `SelfUpdate::Exempt` row.** It doesn't apply,
because `repeat` builds its result from `x`'s block. That is exactly the case
the exemption excludes.

## Phases

> **NOTE — keep the checkboxes current as you go.** Tick `- [x]` in the same
> commit as the work; `- [~]` for partial; strike moot tasks with evidence; fill
> `Commit:` the moment a phase lands. **An unticked box means NOT DONE.**

### Phase 1 — census rows (red)

- [ ] Confirm the string arm declines a 4-argument `repeat` before emitting
      anything. Read `string_self_update.rs`'s arity check, and record the
      result in §2 Verified properties.
- [ ] Add the `cases.tsv` row
      `collections::repeat(value AS List OF T, …) AS List OF T	arm	List OF Integer = [1, 2, 3]	x = collections::repeat(x, 7, 3)	showInts(before)`.
      Copy the signature exactly as `mfb man collections repeat` renders it.
- [ ] Add a growing row: `x = collections::repeat(x, 7, 2, len(x))`.
- [ ] Add a `String`-element row.
- [ ] Add the two failure cases to `tests/runtime/rt_inplace_failure_atomic.rs`.

Acceptance: the census guard passes (the row exists), and the runtime
harness fails the new rows on the `arm` bound, because the value path still
copies.
  Check: `cargo test --test inplace_self_update_census` → pass; `cargo test --test rt_inplace_self_update repeat` → fails on the bound (est. 4 min).
Commit: —

### Phase 2 — the arm

- [ ] Add `ArmId::Repeat` in `self_update.rs`, with its `SELF_UPDATE_ARMS`
      row, `markers` entry and `SELF_UPDATE_TABLE` row.
- [ ] Implement `try_inplace_repeat_assign` in `builder_inplace_assign.rs`
      as §3 describes.
- [ ] Add `"collections.repeat"` to `COLLECTION_SELF_MUTATORS`, and
      `("collections.repeat", "repeat")` to
      `self_update_builtin_names_every_spelling`.
- [ ] Regenerate `field_expect.tsv` with `field_expect_gen.py`. Review each
      new line: an arm at every site, except a growing call on a field that is
      not the last inlined one, which rebuilds.

Acceptance: the Phase 1 rows pass the `arm` bound and the failure-atomicity
cases pass.
  Check: `cargo test --test rt_inplace_self_update && cargo test --test rt_inplace_failure_atomic` → pass (est. 6 min);
  `cargo test --bin mfb self_update` → the table and census unit tests pass (est. 3 min).
Commit: —

### Phase 3 — spec sync and example adoption

- [ ] `src/docs/spec/memory/05_collections.md` `### repeat`: add the in-place
      path (validation before any write, one grow, overwrite then extend, and
      the field-site rule). Cite `try_inplace_repeat_assign`.
- [ ] `examples/wind/src/flow.mfb`: replace the slot loop in `placeParticle`
      and the per-list appends in `seedSwarm` with `collections::repeat`.
      Verify with the `--debug` report and a launch, as in the wind cleanup
      (e779d1bfe).

Acceptance: the spec citations resolve; wind builds, runs and draws, and its
alloc count per frame does not rise.
  Check: `scripts/spec-census.sh --citations` → `MISS-SYMBOL 0` (est. 1 min); wind `--debug` `arena.0.alloc_calls` over a fixed 10 s run, before and after (est. 3 min).
Commit: —

## Validation Plan

- Tests: `cases.tsv` rows (in place, growing, `String`),
  `rt_inplace_failure_atomic.rs` cases, `field_expect.tsv` lines, and
  plan-153-A's fixtures unchanged.
- Coverage check: `grep -c "collections::repeat" tests/runtime/inplace_self_update/cases.tsv` → 3.
- Runtime proof: the `rt_inplace_self_update` output for the `repeat` rows,
  and wind's `--debug` report.
- Doc sync: `mfb spec memory collections`.
- Final gate (once, for all of plan-153):
  `scripts/test-accept.sh target/debug/mfb target/accept-actual` plus
  `cargo test` (est. 40 min; it is the only run that covers every golden a
  shared-codegen change can move, `.ai/testing-gates.md`).

## Open Decisions

- None beyond plan-153-A's name decision, which must be settled before either
  sub-plan starts.

## Corrections

(none yet)

## Summary

The work is plumbing onto an established arm pattern. The risk is evaluation
order when `value` reads `x`, and the census makes a missing or wrong arm
fail loudly.
