## Optimizations

Here's the updated table with a **Stage** column mapped to your pipeline. Key reasoning: **Opt1 (NIR)** = high-level, language-aware, works without CFG/SSA (tree/linear rewrites, inlining, loop restructuring on structured IR). **Opt2 (MIR)** = anything needing CFG, SSA, def-use, or dataflow analysis. Some things live outside both gates (regalloc, machine-code emission).

The **Imp** column is what is **implemented today**, audited 2026-08-30 against the tree (not a wishlist — every mark below cites its code): **Y** = implemented; **~** = partially implemented, with the gap named in the row; blank = not implemented. It is deliberately sparse — **70** rows are `Y` and **6** are `~` out of 189 (`awk -F'|' '/^\| [YN~]/ {gsub(/^ +| +$/,"",$3); if($3=="Y") y++; else if($3=="~") p++; tot++} END {print y, p, tot}' planning/optimizations.md`). Re-audit it with a command, never from memory.

The **Ok** column answers "would this work for *this* compiler, given the plan-100 pipeline?" — **Y** = applicable and hostable in the pipeline (even if it needs net-new infra like a machine model or a vectorizer); **N** = fundamentally inapplicable because MFB lacks the property it exploits. The N rows are inapplicable for a concrete reason — a *missing feature*, not a weakness: no undefined behavior (UB-based), no null model (null-check), no emitted zero-extensions to remove (AArch64 subreg-zext), no hardening to omit (stack-protector), no reference counting (RC-op elimination), no tracing GC / write barriers (write-barrier elimination), and no async/coroutines (state-machine optimization). Read the **Feasibility notes** under the table — several Y rows carry a hard MFB-specific constraint (checked-overflow trapping) that limits *how* they may fire. **Level 0** marks rows that run unconditionally — mandatory lowering and per-build infrastructure (register allocation, base instruction selection), plus any rewrite the language definition requires (FMA contraction). They are not dial passes and carry no level guard.

## The Scale

Here's a 1–5 **risk/safety scale** for your compiler, where the number reflects how much an optimization can distort program behavior, introduce bugs, or destroy debuggability — not how expensive it is to run.

### Level 0 — Always on (required for semantic correctness)
> *"Not an optimization. Omitting it produces a **wrong** program, or none at all."*

- **Side effects:** None *relative to the language definition* — but these passes are load-bearing *for* that definition. A Level-0 row is not a speed/size tradeoff the user may decline; skipping it changes observable behavior or fails the build outright.
- **Error risk:** n/a as a *dial* risk. The risk is the inverse of every other level: the danger is **not running it**.
- **Debuggability:** Fully preserved.
- **Character:** Mandatory lowering and the compiler infrastructure every build needs, plus any rewrite the *spec* requires. The distinguishing test is not "is it cheap or local?" but **"is the un-transformed program still correct?"** — if the answer is no, the row is Level 0 no matter how aggressive it looks.
- **Examples:** FMA contraction (rounds once — see below), most-negative-literal folding, AArch64 conditional-branch relaxation, base instruction selection, register allocation.

**Level 0 is not gated in code.** `level_enabled(0)` is `0 <= active_opt_level()`, which is true at *every* level including `-O0`, so a guard on a Level-0 row could never fail — these passes carry no guard at all. The level number exists to **classify** them, not to switch them, and to give the table one honest home for "runs unconditionally" instead of leaving it implicit. (This subsumes the former "—" marker.)

> **Why FMA contraction is Level 0 and not Level 1 (plan-100).** It looks like a Level-1 peephole — a local, single-expression instruction combine. It is not, because it **rounds once from the exact product instead of twice**, so it changes *which float values exist*. With `a = 1.5e308`, the separate `a * 2.0` rounds to `+inf` and `LET r AS Float = a * 2.0 - a` traps `ErrFloatOverflow`, while the fused `fmsub` yields a finite `1.5e308`. Two fixtures pin that pair as a contract (`rt-behavior/arithmetic/float-fma-fusion`, `rt-error/arithmetic/arithmetic-float-fma-observed-rt`) and the rule is stated in `mfb spec language types` §4.1, so the contracted form *is* the language. Putting it on the dial made `-O0` silently emit different float results — and since `-O0` is a **safety** request, that is the same violation Level 6 exists to prevent, just pointing the other way. **Sort by "is the un-transformed program still correct?", not by how local the rewrite looks.**

### Level 1 — Transparent
> *"The code is identical in every observable way, just less wasteful."*

- **Side effects:** None possible. The transformation is a pure identity rewrite — same values, same operations executed, same order of observable events.
- **Error risk:** Essentially zero; correctness is provable locally without any program-wide analysis.
- **Debuggability:** Fully preserved. Source-to-machine mapping intact, every variable inspectable, breakpoints land where expected.
- **Character:** Local, single-expression or single-instruction rewrites needing no analysis infrastructure.
- **Examples:** constant folding, algebraic simplification (`x+0` → `x`), division-by-constant lowering, jump table generation.

### Level 2 — Tidying
> *"Code is removed or merged, but everything that runs behaves identically."*

- **Side effects:** None on program output, but code/data *disappears* — unused stores, unreachable blocks, duplicate constants.
- **Error risk:** Very low; requires only simple, well-understood analysis (liveness, reachability within a function).
- **Debuggability:** Mildly degraded. Some variables show as "optimized out," some source lines have no corresponding instructions, but stepping still mostly makes sense.
- **Character:** Removal and deduplication based on straightforward local/intraprocedural facts.
- **Examples:** dead-code elimination, dead-store elimination, unreachable code elimination, basic block merging, copy propagation, constant/string merging.

### Level 3 — Restructuring
> *"Computations are moved, shared, and reordered — the result is the same, but the shape of execution is not."*

- **Side effects:** Execution order changes; expressions compute at different times/places than written. Observable behavior preserved *only if the analyses are correct*.
- **Error risk:** Moderate. Correctness depends on nontrivial infrastructure — SSA, dominance, alias analysis, loop analysis. Bugs here are subtle miscompiles, not crashes in the compiler.
- **Debuggability:** Significantly degraded. Stepping jumps around, values exist before their source line, variables merge or vanish. Stack traces still truthful.
- **Character:** Dataflow-driven code motion and redundancy elimination within a function.
- **Examples:** GVN/CSE, LICM (hoisting only trap-free/proven ops), SCCP, PRE, store-to-load forwarding, loop rotation/unswitching, if-conversion.

### Level 4 — Boundary-crossing
> *"Function and data boundaries the programmer wrote no longer exist."*

- **Side effects:** Call structure, stack frames, signatures, and data layout are rewritten. Program semantics preserved, but the runtime *structure* no longer matches the source.
- **Error risk:** High. Interprocedural analysis is where miscompiles hide — escape analysis mistakes, alias assumptions across calls, ABI subtleties. Bugs are hard to reproduce and localize.
- **Debuggability:** Severely degraded. Stack traces lie (inlined frames, eliminated tail calls), functions don't exist, struct layouts differ from declarations, breakpoints on a function may never fire.
- **Character:** Interprocedural and layout-changing transforms.
- **Examples:** inlining, tail-call optimization, function specialization/cloning, dead argument elimination, SROA, structure field reordering, heap-to-stack promotion, devirtualization, LTO.

### Level 5 — Aggressive (code-expanding / deepest analysis)
> *"Same observable results, but the code volume and analysis depth explode."*

- **Side effects:** Behavior is *preserved*, but code volume changes radically (unrolling, vectorization) and correctness now rides the deepest analyses (dependence / trip-count) where a bug silently corrupts data. Includes performance risk: can make code *slower*.
- **Error risk:** Very high — a dependence-analysis bug is a silent miscompile, not a crash.
- **Debuggability:** Effectively gone. One source line becomes 50 SIMD instructions; loops don't iterate the number of times written.
- **Character:** Massively code-expanding or deep-analysis transforms — but still **behavior-preserving**, so they stay on the numeric dial.
- **Examples:** aggressive unrolling, auto-vectorization, SLP, software pipelining, polyhedral transforms, auto-parallelization.

### Level 6 — Semantic-relaxing (opt-in, *off* the numeric dial)
> *"The compiler is allowed to change what the program observably does."*

- **Side effects:** Observable behavior can change — a different floating-point result, or a trap (`ErrOverflow` / `ErrFloatNaN` / `ErrIndexOutOfRange`) that fires at a different time, on a different path, or not at all. Under MFB's checked-overflow, precise-trap semantics these are *behavior* changes, not risk gradations.
- **Error risk:** By definition the transform is *permitted* to produce a program that behaves differently from the source. "Correct" here means "correct under the relaxed semantics the user explicitly accepted."
- **Debuggability:** N/A — the observable program itself differs.
- **Character:** Semantic relaxation. **Never implied by the numeric dial, not even at its maximum (`-O5`).** Requesting maximum *performance* must not silently opt you into different *results*; Level 6 is reached only by explicitly asking for it (`-O6`). This is why fast-math and the trap-order-affecting (†) passes were pulled off the 1–5 dial into their own tier rather than exposed as orthogonal `--fast-math` / `--relax-trap-order` flags.
- **Examples:** fast-math / FP reassociation, integer reassociation & expression-tree balancing (change which op overflows), loop strength reduction (the tail add can trap where the source never multiplied), loop deletion of a trap-capable loop, speculative hoisting of a trapping op. (UB-based optimization would live here too, but MFB has no UB to exploit — see the table.)

> **Note on the "safe form" of † passes.** Each † pass has a proof-gated variant that fires only when range/trap analysis proves trap-freedom — that variant *is* behavior-preserving and may run on the numeric dial (e.g. LICM at L3 hoisting only trap-free ops, or deleting a proven-trap-free loop at L2). Level 6 is specifically the *unproven / relaxing* form that fires anyway. Enabling Level 6 demand-drives the range/trap analysis (a Plan2 prerequisite — see plan-100) so the compiler minimizes how much it actually relaxes.
>
> **The dial-safe alternative: loop versioning.** For the loop-based † passes there is a third option that keeps precise semantics *and* stays on the numeric dial — emit two loop copies under a runtime "no-trap-possible in this range" guard and run the relaxed/vectorized form only in the proven copy (see the "Loop versioning" row). It is behavior-preserving by construction, so it is MFB's principal route to the Level-6 payoff without the Level-6 opt-in; the cost is code size + the guard, not correctness.

## Summary table

| Level | Name | Side effects | Miscompile risk | Debuggability | Analysis needed |
|---|---|---|---|---|---|
| 0 | **Always on** (required for correctness) | none — but **omitting it** changes behavior or fails the build | n/a (the risk is *not* running it) | perfect | none |
| 1 | Transparent | none | ~zero | perfect | none |
| 2 | Tidying | code disappears | very low | good | trivial (liveness) |
| 3 | Restructuring | execution reordered | moderate | poor | SSA, dominance, alias |
| 4 | Boundary-crossing | structure rewritten | high | very poor | interprocedural |
| 5 | Aggressive | code volume explodes (results same) | very high | none | deepest (dependence / trip-count) |
| 6 | Semantic-relaxing (opt-in) | **behavior may change** | n/a — user-blessed | n/a | trap/FP proof, to *minimize* relaxation |

**Level 0 is unconditional** — on at every level including `-O0`, and not gated in code. Levels **1–5 are the numeric performance dial** — enabled cumulatively by `row.level <= active_opt_level()`. **Level 6 is orthogonal**: never implied by the dial (not even `-O5`/"max"), reached only by explicitly requesting it.

The dial's contract, stated once: **`-O0`..`-O5` change the emitted code, never the observable results.** A row is eligible for the dial only if it is behavior-preserving *by construction*. A pass that can change a value or a trap belongs at **Level 0** (if the language requires it) or **Level 6** (if the user must opt in) — never in between. plan-100 learned this the hard way with FMA contraction.

## Design notes for your compiler

- **Level ≠ stage; gate per row, not per seam.** Each seam runs `rows.filter(r => r.level <= active_opt_level() && enabled(r))`. A single `-ON` lights up rows in *both* the Opt1 and Opt2 seams — a seam is never simply "on/off." (This supersedes plan-100's original binary `O0 => skip bracket` / `O1 => run bracket` shape.)
- **Cumulative dial:** `-O3` enables levels 1–3. `-O5` is the maximum *behavior-preserving* level.
- **The default is `-O1`, not `-O0` (plan-100).** MFB's shipping codegen already runs two Level-1 passes (the post-regalloc machine peepholes), so plan-100 sets the dial's default to `-O1` — keeping the default byte-identical to today — and makes **`-O0` the explicit "all dial passes off"** baseline (a correct-but-unoptimized path, not a byte-identity target). Those two passes are the **first landed rows** and are gated by `level_enabled(1)`; see the annotated rows below. Both are strictly behavior-preserving, so **`-O0` produces identical program behavior** — only slower/larger code. FMA contraction was evaluated for the dial and deliberately left OFF it: it changes float results, so it is mandatory lowering, not an optimization (see the "Instruction selection / combining" row).
- **Level 0 is already always on**; **Levels 1–2 are your "always on" *candidates*** — even a debug build could run them with little downside. (Level 1 *is* the default-on set today.)
- **Level 4 is the debuggability cliff** — stack traces stop being truthful; a natural default ceiling for development builds, with 4–5 reserved for release.
- **Level 6 is orthogonal and opt-in** — *never* implied by `-O5` or an "auto/max" setting; the user must name it (`-O6`). Cranking the dial for speed must never silently change results.
- **Prerequisites are not dial rows.** SSA construction (mem2reg), alias analysis, range/trap analysis, memory-SSA, loop canonicalization, and function-attribute (`no-trap`) inference are Plan2 infrastructure, built *on demand* when an enabled pass needs them (LICM needs trap-analysis to hoist safely; alias-based passes need alias/memory-SSA; L6 rows need range analysis to minimize relaxation). They live in plan-100's Plan2, not in this table. The first of them exists: SSA landed with the propagation rows as the **overlay** in `src/optimizer/opt2/plans/ssa.rs` (dominators, semi-pruned phis, per-use value resolution, copy-forwarding facts over the allocator's own CFG/effect model; the stream keeps its vregs, so there is no out-of-SSA lowering and regalloc is untouched).
- **Definition — "dead" vs "unreachable" code are different properties, hence two rows.**
  *Dead* code is reachable but useless: it executes (or would), yet nothing consumes its
  results and it has no observable effect — `LET a = x` with `a` never read. The analysis
  question is *"does anyone use this?"* (def-use/liveness), and under MFB's checked
  semantics DCE may remove only code **proven trap-free** (a reachable-but-unused `x + y`
  stays: it can still raise `ErrOverflow`, and an unused Float bind is itself the §4.1
  observation boundary). *Unreachable* code is the opposite: its results may look used,
  but control flow can never arrive — statements after `RETURN`, the arm of a folded
  constant branch, CFG blocks with no path from entry. The analysis question is *"can
  execution get here?"* (reachability), and the trap gate **vanishes**: unreachable code
  cannot trap because it never runs, so even an unreachable `x / 0` is removable.
  Neither row subsumes the other, and they have different producers: DCE feeds off value
  rewrites (folding strands dead feeders), while unreachable-code elimination feeds off
  branch simplification/folding (folding `IF FALSE` is what *creates* statically-dead
  branches) — which is why those two rows are a natural landing pair.
- **Size (`-Os`/`-Oz`) is a second, orthogonal objective — not a dial level.** It doesn't fit the 1–6 risk axis; it re-weights *profitability* (inlining/unrolling budgets shrink, outlining/error-path-dedup/shared-trap-stubs become more profitable, layout favors density). Model it like a target flag that shifts each pass's cost model, composed with a numeric level (e.g. `-Oz` + level-3 safety). Net-new — MFB has no `-O*`/size axis today.


| Ok  | Imp | Name | Level | Stage | Description |
| --- |-----|---|---|---|---|
| Y   | Y   | Constant folding                               | 1 | Opt1 + Opt2 | Evaluate constant expressions at compile time; rerun in Opt2 as other passes expose constants. **LANDED, both halves, gated Level-1. Opt1: `src/optimizer/opt1/constant_folding.rs` (shared `local_rewrites` walk) folds the provably NON-trapping subset per this table's own reclassification note (a constant overflow / `/0` / `MIN/-1` / out-of-range Byte keeps its runtime trap): Integer+Byte `+ - * / MOD ^` and unary `-`, Boolean `AND OR XOR NOT`, String `&`, same-type Integer/Byte/Boolean/String comparisons. Opt2: `src/optimizer/opt2/constant_folding.rs`, a block-local folder on the selected pre-regalloc MIR (`mov_imm`-fed wrapping ALU chains → `mov_imm`; trap preservation is structural — checked user arithmetic lowers to the unmodeled flag-setting ops). Still excluded in both: Float/Fixed/Money (bit-exact constant channel needed — the backend's naive literal parser is ~1 ULP off — plus rounding modes) and cross-type §4.1-promoting pairings; binding-level constant *propagation* is the separate row below.** |
| Y   | Y   | Most-negative-literal folding                  | 0 | Opt1 (`ir::lower`) | Fold a unary minus into the literal when the *positive* magnitude does not fit the type but the negated form does — `Integer` `-9223372036854775808`, and the `Fixed`/`Money` raw-i64 equivalents. **Level 0, not an optimization: without it the `Unary` shape survives to codegen, which materializes the u64 bit pattern and negates it at runtime — an `ErrOverflow` on every run (bug-286). IMPLEMENTED: `ir/lower.rs:3360-3400`; the guards are exact, firing only at the one representable-when-negated value, so every in-range literal keeps its `Unary` shape.** |
| Y   | Y   | Constant propagation                           | 2 | Opt2 | Replace variables known to hold constants; needs def-use/SSA to do properly. **LANDED at Level 2 on the Plan2 SSA overlay (`src/optimizer/opt2/plans/ssa.rs`: forward CHK dominators over the allocator's own CFG, semi-pruned phi placement — Briggs globals plus every copy-source variable — and a dominator-tree renaming walk; it is an *overlay*, the stream keeps its `%vN` registers and no out-of-SSA lowering runs before regalloc). `src/optimizer/opt2/constprop.rs` computes a pessimistic per-SSA-value constant lattice through the very same `fold_one` rules as the L1 block-local folder (shared function — the two rows cannot drift) plus all-arguments-agree phis, then rewrites modeled instructions to `mov_imm` — exactly the cross-block/join constants the block-local folder must forget at a label. Trap preservation is structural (flag-setting checked arithmetic is `Step::Barrier`, never folded); loop-carried phis stay unknown (pessimistic fixpoint, sound). Stranded feeders are DCE's job.** |
| Y   | Y   | Copy propagation                               | 2 | Opt2 | Replace uses of a variable with its source copy; trivial on SSA. **LANDED at Level 2 on the same SSA overlay: the renaming walk records, per use whose value came from a register copy (`mov` / `fmov_d_from_d`), the copy chain's *ultimate* source operand — only when the source provably still holds the same SSA value at that point (a redefinition on any intervening path surfaces as a phi and invalidates the forward; copy-source variables get full phi placement precisely so this check is meaningful at non-use points). `src/optimizer/opt2/copyprop.rs` is then a pure use-field substitution (`dst` never touched, no instruction added/removed/reordered — trivially behavior-preserving); the bypassed copies become dead and the DCE row sweeps them.** |
| Y   | Y   | Dead-code elimination (DCE)                    | 2 | Opt1 + Opt2 | Remove unused code; simple tree-level DCE in Opt1, precise SSA-based DCE in Opt2. **LANDED, both seams, Level 2. Opt1: `src/optimizer/opt1/dce.rs` + the `opt1/plans/reads` name census — removes unused scalar binds/evals whose initializers are provably pure AND trap-free (leaves, comparisons, Boolean logic; arithmetic can raise `ErrOverflow` and an unused Float bind is itself the §4.1 observation boundary, so both stay), fixpoint for chains, scope-blind census so shadowing is conservative. Opt2: `src/optimizer/opt2/dce.rs` + `opt2/plans/mark` — "assume dead, prove live" over the selected stream using the allocator's own effect model (`regalloc::class_models`/`effect`), now in the row's **precise SSA-based form**: marking consumes the `opt2/plans/ssa` overlay, so a live use keeps alive only the definitions that actually *reach* it (its SSA value's defining instruction, or every phi argument through a join) — an unread redefinition of a live vreg name dies, which the old all-defs-of-a-used-name marking had to keep. Uses without SSA facts (unreachable blocks) fall back to the conservative all-defs edge. Flag-setters are never candidates, so checked arithmetic survives structurally.** |
| Y   | Y   | Dead-store elimination                         | 2 | Opt2 | Remove stores overwritten before read; needs dataflow/alias info. **LANDED at Level 2 as the cross-block dataflow form — `src/optimizer/opt2/dse.rs`: a backward must-analysis over the allocator's own CFG (dead-slot sets; meet = intersection over successors, so a store dies only when *every* path fully overwrites its 8 bytes before any possible read; exit blocks start empty), keeping the store-to-load-forwarding safety mold per instruction (full-slot `sp` stores/loads modeled; pure ALU/compares, labels, and memory-free branch terminators pass through; any other memory op, call, or unknown clears the state, so mis-modeling can only lose an elimination — an aliasing access path into the frame necessarily starts with an op outside the neutral set). The former block-local subset falls out as the single-block case. Broadening beyond `sp` slots (arbitrary bases, memory-SSA) is Plan2 alias infrastructure per the "Prerequisites are not dial rows" note, arriving with the alias-based rows below.** |
| Y   | Y   | Unreachable code elimination                   | 2 | Opt1 + Opt2 | Prune statically-dead branches in Opt1; prune unreachable CFG blocks in Opt2. **LANDED at Level 2, both seams, gap closed. Opt1: `src/optimizer/opt1/uce.rs` truncates post-terminal statements (after RETURN/FAIL/EXIT PROGRAM/EXIT/CONTINUE, an IF whose both arms terminate, an ELSE-bearing MATCH whose cases all terminate), keeping a function's final RETURN **and every `TRAP` handler in the dropped range** — a trailing handler is the *raise* path's destination (`FAIL e ; TRAP(err)…` transfers INTO it), so post-terminal ≠ unreachable for it; deleting one un-swallowed `control-flow-behavior`'s error 11 at `-O2` until the exception landed with the loop rows. No trap gate otherwise — unreachable code never executes. Runs before tree-DCE so names read only from unreachable code become collectible. Opt2: `src/optimizer/opt2/uce.rs` prunes CFG blocks unreachable from entry, with an over-approximated root set (any label referenced by ANY instruction field keeps its block), labels included. The formerly-named gap — the "statically-dead branches" half (constant conditions) — landed with the branch-simplification row: Opt1 `branches.rs` drops the untaken arm itself, and Opt2 `branches.rs`'s folds are exactly what feeds this row's CFG pruning.** |
| Y   | Y   | Algebraic simplification                       | 1 | Opt1 | Apply identities (`x*1` → `x`); pure local rewrite, no analysis needed. **LANDED: `src/optimizer/opt1/algebraic.rs`, gated Level-1 on the Opt1 seam. Identity-element rewrites only — `x+0`/`x-0`/`x*1`/`x/1`/`x^1` (Integer/Byte), `x*1.0`/`x/1.0`/`x-0.0` (Float — the exact-IEEE subset; `x+0.0` maps `-0.0`→`+0.0` so it stays), `x AND TRUE`/`x OR FALSE`/`x XOR FALSE`, `s & ""`, `NOT NOT x`. Operand-dropping folds (`x*0`, `FALSE AND x`, `x MOD 1`) belong to the constant-folding/branch-simplification rows; numeric rewrites require the surviving operand's scope-known type to equal the constant's (else dropping the op changes §4.1 promotion), and Fixed/Money are excluded (raw-representation kernels + rounding modes).** |
| Y   | Y   | Strength reduction (non-loop)                  | 1 | Opt1 | Replace expensive ops with cheaper ones (`x*2` → `x<<1`); local rewrite. **LANDED: `src/optimizer/opt1/strength.rs`, gated Level-1 on the Opt1 seam (shared `local_rewrites` walk). Trap-identical rewrites only — `x*2`/`2*x` → `x+x` (checked add replaces the smulh-based checked-multiply sequence) and `x^2` → `x*x` (one checked multiply replaces the `emit_integer_pow` loop, which computes `(1*x)*x`), Integer/Byte, pure duplicable operands (Const/Local/Global/Capture) with the constant's type (§4.1 promotion gate). Excluded, with reasons in the module doc: `x / 2^k` → shift (truncation-vs-floor fixup belongs to Division-by-constant lowering), Float `x/C` → `x*(1/C)` (blocked on a bit-exact Float-constant channel — the naive literal parser is ~1 ULP off), higher powers (code-expanding; needs a cost model).** |
| Y   | Y   | Peephole optimization                          | 1 | Opt2 / post-regalloc | Local pattern rewrites; MIR peepholes in Opt2, machine peepholes after regalloc. **LANDED (plan-100): the post-regalloc machine peepholes `forward_stores_to_loads` + `remove_fp_shuttles` are gated Level-1 rows in `src/optimizer/opt2/` — on at `-O1`, off at `-O0`.** |
| Y   | Y   | Branch simplification / folding                | 2 | Opt1 + Opt2 | Fold known conditions; structured version in Opt1, CFG version in Opt2. **LANDED, both seams, Level 2. Opt1: `src/optimizer/opt1/branches.rs` — a literal `Const Boolean` condition (constant folding's product, so it runs right after `local_rewrites`) splices `IF TRUE/FALSE` to its taken arm and drops `WHILE FALSE` whole; the discarded arm is *unreachable* per the definition note, so no trap gate applies to it. Deliberately unfolded: `DO…UNTIL TRUE` (splicing would re-target `EXIT`/`CONTINUE`), `WHILE TRUE`, constant `MATCH`. Opt2: `src/optimizer/opt2/branches.rs` — block-local in the constant folder's mold (known map advanced through the shared `fold_one`), plus one riding fact: the last `cmp`/`cmp_imm`'s operands when both known; a conditional flag branch reached with the fact folds to `b target` (taken) or is deleted (fallthrough). Flags are trusted ONLY from `cmp`/`cmp_imm` — checked arithmetic's `adds`/`subs` are `Barrier`s and `b.vs`/`b.vc` are additionally unmodeled, so overflow guards can never fold. The newly-dead blocks are UCE's food (the pairing the definition note names).** |
| Y   | Y   | Jump threading                                 | 3 | Opt2 | Redirect jump-to-jump chains; requires CFG. **LANDED at Level 3, the hop-collapsing core: `src/optimizer/opt2/threading.rs` — a `label L; b M` pair is a trampoline; every *terminator* `target` field resolves its chain to the final label (cycle guard: a chain hitting a trampoline cycle — an intentional spin — is left entirely alone; non-terminator label references are never rewritten). Trivially behavior-preserving (a label emits nothing and the hop executes nothing observable); orphaned trampolines are then pruned/fused by UCE and block merging. The duplicate-the-block conditional form (threading through correlated conditions) is the row's future extension.** |
| Y   | Y   | Basic block merging                            | 2 | Opt2 | Merge single-pred/single-succ blocks; requires CFG. **LANDED at Level 2, running last in the seam: `src/optimizer/opt2/merge.rs` — (1) branch-to-next elimination (a terminator, conditional included, whose target is the very next instruction's label transfers where fallthrough already goes: deleted); (2) unreferenced-label removal (census = `opt2::uce`'s over-approximation, any field of any instruction; sound because the encoder's label table is per-function — `encode_plan` clears it between functions — and the index-0 entry label is kept unconditionally). Both delete only instructions that execute nothing, fusing the single-pred/single-succ pairs the folded branches and threaded jumps strand.** |
| Y   |     | Tail-call optimization                         | 4 | Opt2 | Convert tail calls to jumps; needs call-position analysis on CFG, affects frame layout before regalloc. |
| Y   | Y   | Common subexpression elimination (CSE)         | 3 | Opt2 | Reuse repeated computations; subsumed by GVN on SSA. **LANDED via the GVN row, exactly as this row's own description says: `src/optimizer/opt2/gvn.rs` (dominance-scoped) + the block-local complement `opt2/lvn.rs`. Shares those rows' scope: pure int-vreg whitelist expressions, dominance-proven availability (fresh single-def holders when the candidate's register is multi-def); the partially-redundant remainder is the PRE row.** |
| Y   | Y   | Local value numbering                          | 3 | Opt2 | Block-local redundancy elimination on MIR. **LANDED at Level 3: `src/optimizer/opt2/lvn.rs` — name→value-number + normalized-expression tables over int-vreg space only (a physical operand is ineligible: hidden clobbers — the x86 `mul` expansion, calls — could stale it invisibly, while vreg values legitimately survive a `bl`, so dedup works across calls). Equal Integer literals share numbers; commutative ops sort operand numbers (`a+b` ≡ `b+a`); kills come from the allocator's own `effect` defs so a stale holder can never be reused; a label clears everything. Candidates are the pure flag-free whitelist minus the moves — checked arithmetic is never touched — and a recompute becomes `mov dst, holder`, which copy propagation bypasses and DCE sweeps.** |
| Y†  |     | Reassociation                                  | 6 | Opt1 + Opt2 | Reorder associative ops; canonical form + ILP. **†off the dial: changes which int op overflows and changes FP results — Level 6 opt-in** |
| Y   | Y   | Global value numbering (GVN)                   | 3 | Opt2 | Whole-function redundancy elimination; requires SSA. **LANDED at Level 3 on the SSA overlay: `src/optimizer/opt2/gvn.rs`. Expression keys are SSA value ids *canonicalized* through copy chains and `mov_imm` Integer feeders (two `mov_imm 8`s are distinct SSA values but one constant — measured zero table hits across the example corpus before canonicalization, 108–311 rewrites per big example after); commutative ops sort operands. A recompute reuses a candidate only when the candidate's block **dominates** it (O(1) via the overlay's dominator-tree preorder intervals). Dominance is the whole availability proof for the *value* (an operand redefinition between the candidate's last execution and the reuse would imply a candidate-avoiding path, contradicting dominance); the *register* is handled by cases — a single-def dst is read directly, a multi-def dst gets the value parked in a minted fresh single-def holder right after the candidate. Both checks are phi-placement-independent. Int-vreg space only, pure whitelist minus moves — trap discipline structural. Arm-and-arm join redundancy (no dominator) is the PRE row's.** |
| Y   | Y   | Sparse conditional constant propagation (SCCP) | 3 | Opt2 | Combined constant prop + unreachable pruning; classic SSA algorithm. **LANDED at Level 3 on the SSA overlay — `src/optimizer/opt2/sccp.rs`, the Wegman–Zadeck optimistic fixpoint: every value starts `Top` and every edge unreachable but the entry's, then constants and reachable edges are discovered together. A phi meets only its **reachable** arguments (the overlay's phis now carry their predecessor block for exactly this), so a constant survives a join whose other arm is provably dead — which the pessimistic Level-2 constant-propagation row cannot prove. Rewrites: constant results become `mov_imm` via the shared `fold_one` (so checked arithmetic is structurally unfoldable), and decided branches become `b`/fall-through using the same `branches::verdict` table as the L2 row. Two traps found by its own tests: conflating `Top` with `Bottom` when evaluating a branch commits the dead edge before the constant is known (fixed: an undecided compare commits NO edge), and live-in values are never re-evaluated so they must START at `Bottom` — left optimistic, every compare against a parameter deferred forever and a loop counter folded to its first-iteration value.** |
| Y   | Y   | Loop-invariant code motion (LICM)              | 3 | Opt2 | Hoist invariant code from loops; needs CFG loop analysis + SSA. (A simple structured-loop version can also live in Opt1.) **at L3 hoists only trap-free/proven ops; unconditional hoist of a trapping op is Level 6** **LANDED at Level 3 as the structured-loop (Opt1) form this row's own parenthetical blesses — `src/optimizer/opt1/licm.rs` + the `opt1/plans/loops` fact base: moves a scalar `LET` out in front of its loop when the initializer is in the pure non-trapping class (dce's — no arithmetic, so a zero-trip loop evaluating it once anyway observes nothing), invariant (reads no name the body or the loop variable redefine, no globals), the body defines the name exactly once, and every occurrence of the name lives inside the loop (whole-function vs body census — scope-blind, shadow-safe). Fixpoint, so inner-loop hoists ride outward. The CFG/SSA form with range/trap analysis (hoisting proven-trap-free arithmetic) remains the Plan2 extension.** |
| Y   | Y   | Induction variable simplification              | 3 | Opt2 | Canonicalize loop counters; needs SSA loop analysis. **LANDED at Level 3 as duplicate-counter merging — `src/optimizer/opt2/indvars.rs`: a basic induction variable is a header phi whose two arguments are an outside-the-loop initial value and an `add_imm` reading that same phi. Two of them merge when their initial values are equal (compared through GVN's copy/constant canonicalization), their strides are equal, their increments sit in the SAME block (equal execution counts), and the survivor's register has exactly two definitions (init + increment, so it provably holds the counter). Only uses AFTER BOTH increments are redirected — between them the counters legitimately differ by one step. `add_imm` is in the pure whitelist, so a checked (`adds`) counter is never a candidate. The survivor is chosen by lowest register number, and phi placement was made deterministic (it iterated a HashMap) so codegen cannot vary run to run. Strength-reducing derived IVs (`iv * C` → add chains) is the row's larger half and needs a cost model. **Measured: this row fires 0 times across the whole example corpus today** (`mfb build -O3 -v` on browser/ai_chat/etc). That is by construction, not a defect: MFB `FOR` counters lower their increment as a *checked* user-level `+` (`builder_control.rs:1695`, an ErrOverflow-capable `adds`), which the trap discipline excludes, and lowering mints exactly one unchecked iteration temp per loop — so no two mergeable counters exist. It becomes live when unchecked derived counters appear (address/stride counters, or the strength-reduction half above).** |
| Y†  |     | Loop strength reduction                        | 6 | Opt2 | Replace induction-variable multiplies with adds; needs IV analysis. **†off the dial: the tail add can trap where the source never multiplied — Level 6 opt-in** |
| Y   | Y   | Loop unswitching                               | 3 | Opt2 | Hoist invariant conditionals out of loops; duplicates CFG regions. **LANDED at Level 3 as the structured-loop (Opt1) form — `src/optimizer/opt1/unswitch.rs`: a top-level `IF` whose condition is pure, non-trapping, and invariant (loop variable included in the defined set) splits the loop into one up-front test over two specialized copies; the condition now evaluates once instead of per-iteration (or zero times — hence mandatory trap-freedom). Arms duplicate verbatim inside same-kind loops, so `EXIT`/`CONTINUE` bind unchanged, and a duplicated `FOR`/`FOR EACH` header still evaluates once at runtime (one branch runs). NIR locals are FUNCTION-unique (`validate::body` keeps one flat map), so the second copy is cloned **freshened** — every declared name renamed via `plans/loops::freshened_clone`, whose hand-rolled mutable walk is verified against the canonical read-only census (a missed variant skips the transform instead of miscompiling). One unswitch per loop per compile, bodies ≤ 64 ops.** |
| Y   | Y   | Loop rotation                                  | 3 | Opt2 | Convert to do-while form; CFG transformation. **LANDED at Level 3 as the structured-loop (Opt1) form — `src/optimizer/opt1/rotate.rs`: `WHILE c: body` → `IF c THEN DO body UNTIL NOT c`, saving a branch per iteration in the lowered code. Exact evaluation parity (condition n+1 times, body n times, same order), so NO purity/trap-freedom is required of either; `DO..UNTIL` conditions share `WHILE`'s outer-scope evaluation. Structural guards from `builder_control`'s loop-stack discipline: bail when the body carries unshielded `EXIT`/`CONTINUE` of the loop's own kind (they would dangle — the rotated loop pushes `Do`) or of `Do` kind (the new loop would capture an outer `DO`'s). Runs last among the loop rows.** |
| Y†  |     | Loop deletion                                  | 6 | Opt2 | Remove side-effect-free loops; needs SSA use analysis. **†off the dial: a loop that can trap is not side-effect-free — deleting it drops an observable raise; Level 6 opt-in (a proven-trap-free loop is safe to delete at L2)** |
| Y   | Y   | Loop fusion (jamming)                          | 3 | Opt1 | Merge adjacent same-bound loops; far easier on structured NIR loops than on CFG. **LANDED at Level 3 — `src/optimizer/opt1/fuse.rs`. Fusion interleaves phases, so under checked semantics eligibility is a proof, not a heuristic: same loop-var type + structurally identical stable-leaf bounds *resolved through a constant environment* (lowering pre-binds bounds into per-loop `$for_endN` temps and sits those binds between the loops — the pure gap binds slide in front of the fused loop), both bodies flat pure non-trapping statements (`plans/loops::pure_statement` — local binds/assigns only), read/write disjointness both ways (exempting the iteration-mirror binds `LET i = $for_iterN` both bodies carry — provably equal per iteration), and no `TRAP`/by-ref-capture in the function (`locals_survive_a_raise`) — closing the one residual divergence: how many second-body iterations ran when the FOR increment itself overflows (both shapes raise the identical error/loc; without a surviving observer the phase difference in dead locals is invisible). The loops' distinct iteration temps are bridged with a per-iteration pure bind (never a risky tree-wide rename); the second body's mirror binds are dropped (NIR locals are function-unique). Chains fuse across census-refreshed rounds. Broader fusion (calls, arithmetic, memory) waits on Plan2 dependence/trap analysis per the "Prerequisites are not dial rows" note.** |
| Y   | Y   | Loop fission (distribution)                    | 3 | Opt1 | Split loops for locality/vectorization; easiest on structured loops. **LANDED at Level 3 — `src/optimizer/opt1/fission.rs`, fusion's inverse under the identical proof obligations (pure flat statements, both-ways disjoint halves, stable-leaf bounds neither half writes — duplicated bound evaluation is a pure leaf re-read — and the `locals_survive_a_raise` guard for the increment-overflow phase divergence; the split copies keep the same `loc`, so a raise is identical). The second loop re-declares the loop variable and the carried iteration-mirror binds (`LET i = $for_iterN`), so it is a freshened clone (function-unique locals; census-verified rename). Splits at the first boundary yielding two halves of ≥ 2 real statements each, once per loop.** |
| Y   |     | Loop interchange                               | 5 | Opt1 | Swap nested loop order; needs structured loop nests + dependence analysis. **Reclassified 3→5: relies on array dependence analysis (the deep-analysis-silent-corrupt risk that defines L5), even though results are preserved.** |
| Y   |     | Loop tiling / blocking                         | 5 | Opt1 | Block loops for cache; structured-loop transformation. **Reclassified 3→5: dependence-analysis-driven (see interchange).** |
| Y   |     | Loop unrolling                                 | 5 | Opt1 or Opt2 | Replicate loop bodies; simple full-unroll in Opt1, runtime/partial unroll with trip-count analysis in Opt2. |
| Y   | Y   | Loop peeling                                   | 3 | Opt2 | Split off first/last iterations; usually paired with Opt2 loop analyses. **LANDED at Level 3 as the structured-loop (Opt1) first-iteration form — `src/optimizer/opt1/peel.rs`: `WHILE c: body` → `IF c THEN body; WHILE c: body`. Exact duplication, not reordering (condition n+1 evals, body n, same order), so NO purity/trap-freedom is required; guards: no unshielded `EXIT`/`CONTINUE` of the loop's kind (the peeled copy sits outside the loop) and body ≤ 16 ops (growth cap); the copy is a *freshened* clone (NIR locals are function-unique — every name the body declares is renamed, census-verified). One peel per loop per compile; the downstream constant/branch rows then specialize the peeled copy. Last-iteration peeling waits on Opt2 trip-count analysis.** |
| Y   |     | Loop skewing                                   | 5 | Opt1 | Shift iteration space; structured/polyhedral-level transform. **Reclassified 3→5: dependence-analysis-driven (see interchange).** |
| Y‡  |     | Software pipelining                            | 5 | Opt2 (late) | Overlap iterations; needs scheduling info, runs near end of Opt2. **‡net-new machine model** |
| Y   |     | Function inlining                              | 4 | Opt1 | Substitute callee bodies; do it on NIR before Plan1 so storage slots/symbols are computed once for the merged body. |
| Y   |     | Aggressive/heuristic inlining                  | 4 | Opt1 | Profile/cost-model-driven inlining; same stage, bigger budget. |
| Y   |     | Partial inlining                               | 4 | Opt1 | Inline hot early-exit portion; NIR-level function surgery. |
| Y   |     | Interprocedural constant propagation (IPCP)    | 4 | Opt1 | Propagate constant args across functions; interprocedural work fits pre-Plan1. |
| Y   | ~   | Function specialization / cloning              | 4 | Opt1 | Clone functions for known args; must precede Plan1 symbol/slot assignment. (Monomorphization already exists — reuse it.) **~ Partial: monomorphization clones per concrete *type* instantiation, not per known *argument value*; value-based specialization is the net-new part.** |
| Y   |     | Argument promotion                             | 4 | Opt1 | Pass values instead of pointers; changes signatures → before Plan1. |
| Y   |     | Dead argument elimination                      | 4 | Opt1 | Remove unused params; changes signatures → before Plan1. |
| Y   |     | Return value propagation                       | 4 | Opt1 | Propagate known returns into callers; interprocedural, NIR-level. |
| Y   |     | Devirtualization                               | 4 | Opt1 | Resolve indirect calls to direct; enables Opt1 inlining. (Targets `FUNC` indirect calls.) |
| Y   | ~   | Escape analysis                                | 4 | Opt1 | Determine non-escaping objects; results feed Plan1 storage-slot decisions. (Resource-escape analysis already exists — `src/ir/resource_escape.rs`.) **~ Partial: resource-escape only (`src/ir/resource_escape.rs`); general non-escaping-object analysis feeding Plan1 slot decisions is net-new.** |
| Y   |     | Scalar replacement of aggregates (SROA)        | 4 | Opt1 | Split structs into scalars; must run before Plan1 assigns StorageType/slots. |
| Y   | Y   | Store-to-load forwarding                       | 3 | Opt2 | Replace loads with prior stored values; needs alias analysis. (A block-local machine version already exists — `forward_stores_to_loads`, now `src/optimizer/opt2/peephole.rs`, gated as a **Level-1** machine peephole under the "Peephole optimization" row.) **LANDED: the L3 broadening now ships too — `src/optimizer/opt2/stldfwd.rs` on the pre-regalloc stream, forwarding a store to a load in a *different block* whenever every path between them leaves the slot untouched (the `opt2/plans/memory.rs` availability dataflow, shared with the redundant-load row: one traversal, two counters). Guards: the dataflow's own "anything unmodeled clears everything" discipline and GVN's single-def holder rule. Arbitrary bases / memory-SSA remain Plan2 alias infrastructure.** |
| Y   | Y   | Redundant load elimination                     | 3 | Opt2 | Remove loads of already-available values; SSA + alias analysis. **LANDED at Level 3 — `src/optimizer/opt2/rle.rs` (docs + tests; the pass body is the shared traversal in `stldfwd`, since both memory rows ask the same question of the same dataflow and running it twice doubled the cost for nothing). A reload whose slot still holds an earlier LOAD's value becomes `mov dst, <holder>`. Facts come from `opt2/plans/memory.rs`: a forward must-dataflow over the allocator's CFG (meet = agreement on the same SSA value; any unmodeled instruction — call, other memory op, FP, unknown — clears the whole state), plus GVN's single-def holder rule so the register still holds what the analysis recorded. Beyond `sp` slots needs Plan2 alias/memory-SSA.** |
| Y   |     | Memcpy/memset idiom recognition                | 3 | Opt2 | Replace copy loops with bulk intrinsics; loop analysis on MIR. **BLOCKED on both halves — measured 2026-08-29. (1) No bulk primitive: the neutral MIR has no memcpy/memset op and no runtime helper to call for one (`memcpy` appears only in per-platform bootstrap/app code), so the *replacement* half needs a new op + per-backend lowering like the if-conversion row above. (2) The *recognition* target is not a clean copy loop: a user `FOR i: dst = append(dst, get(src, i))` inlines the collection ops entirely (measured on a probe: 136 `ldr_u64` / 103 `str_u64` and ZERO `bl` calls in the loop), so the body is bounds checks + capacity growth + header updates around the element move, not a recognizable `ldr src+i; str dst+i` pair — proving it equivalent to `memcpy(n)` means proving the growth and aliasing semantics, not pattern-matching two instructions.**
| Y   |     | If-conversion (predication)                    | 3 | Opt2 (late) | Convert branches to selects; CFG diamond pattern matching. (csel/cmov are emittable.) **NOT an optimizer-only row — measured 2026-08-29: the neutral MIR has NO select op (`grep -n "Csel\|Cmov" src/arch/ops.rs` is empty; the "csel/cmov are emittable" note refers to the ISAs, not to this IR). Landing it means a new neutral op wired through `arch/ops.rs` (variant/name/parse/tables), the `mir.rs` mirror lists, the effect model (a select reads two sources AND flags — new field roles) and regalloc, plus lowering in aarch64 (`csel`), x86-64 (`cmov`) and riscv64 (base RV64I has NO conditional move — it needs a branchless expansion), and regenerating `.ncode`/`.ncodesum` goldens on 5 targets. An optimizer-only alternative exists — blend the arms with existing pure ALU ops (`mask = 0 - cond; (a AND mask) OR (b AND NOT mask)`) using the compare's *source* boolean register rather than the flags — but it costs ~5 ALU ops plus executing both arms, so it needs a profitability model, and it is only correct where that register is provably 0/1.**
| Y   |     | Branch layout / prediction hints               | 2 | Opt2 (post-SSA) / codegen | Order blocks for fall-through; late CFG layout, after out-of-SSA. |
| Y   |     | Code layout / block placement                  | 2 | codegen | Arrange hot code contiguously; machine-level. |
| Y   | Y   | Tail duplication                               | 3 | Opt2 | Duplicate small join blocks; CFG transform. **LANDED at Level 3 — `src/optimizer/opt2/tailduped.rs`: a small, terminator-final join entered ONLY by unconditional branches (2+ of them, its label named by nothing else, never itself) is copied into each predecessor, so the block-local rows below keep their facts through the tail instead of forgetting at the merge. Exact duplication, so no purity/trap gate is needed; growth is capped at 8 instructions and nothing is renamed (a duplicated def is just a second def of that vreg, which the SSA overlay and the multi-def rules in GVN/the memory rows already model). The orphaned original is pruned by the unreachable-block row. Measured: 1348 duplications on examples/ai_chat.** |
| Y   |     | Jump table generation                          | 1 | Opt2 / codegen | Lower dense switches to tables; MIR lowering or backend. (Targets `MATCH`/union-tag dispatch.) |
| Y   |     | Switch lowering strategies                     | 1 | Opt2 / codegen | Binary search/bit tests for sparse switches; same as above. |
| Y‡  |     | Auto-vectorization (loop)                      | 5 | Opt2 | SIMD-ify loops; needs SSA, dependence, and trip-count analysis. **‡2-lane packed SIMD exists; wider = new encoders** |
| Y‡  |     | SLP vectorization                              | 5 | Opt2 | Pack straight-line scalar ops into SIMD; SSA-based. **‡2-lane packed SIMD exists; wider = new encoders** |
| Y‡  |     | Instruction scheduling                         | 3 | pre/post-regalloc | Reorder to hide latency; machine-level, around regalloc. **‡net-new machine/latency model** |
| Y   | Y   | Register allocation                            | 0 | regalloc | Assign virtual registers to physical; your dedicated stage. (Exists — linear-scan.) **Infrastructure, not a dial row: the allocator runs at every level; only its aggressiveness/refinements (coalescing, remat, live-range splitting, callee-save selection, spill-code opt) are level-gated.** |
| Y   | Y   | Register coalescing                            | 2 | regalloc | Eliminate copies via shared assignment; part of regalloc (interacts with out-of-SSA copies). **LANDED at Level 2 - `src/codegen/engine/regalloc/linear_scan.rs`: a `mov dst, src` whose source dies at the copy gives the destination a `copy_hint`, and on takeover `active.retain(...)` drops the source's claim FIRST (otherwise its later expiry clears a bit the destination now holds). Self-moves are swept at the end. **Measured: 2557 copies removed on examples/ai_chat at -O3.** |
| Y   | Y   | Rematerialization                              | 2 | regalloc | Recompute cheap values instead of spilling; regalloc component. **LANDED at Level 2 - `linear_scan.rs`: a spilled vreg whose single definition is a register-free `mov_imm` is re-emitted at each use instead of being stored and reloaded, and its now-dead spill store is skipped. **Measured: 36 on examples/ai_chat at -O3.** |
| Y   | Y   | Stack slot coloring                            | 2 | regalloc | Reuse slots for non-overlapping lifetimes; regalloc/frame lowering. **LANDED at Level 2 - `linear_scan.rs`: a linear scan over `slot_end` reuses a spill slot whose previous occupant's live range has ended. **Measured: 7188 slots shared on examples/ai_chat at -O3.** |
| Y   |     | Frame pointer omission                         | 2 | codegen | Free FP register; frame lowering. |
| Y   |     | Shrink wrapping                                | 2 | regalloc / codegen | Sink prologue/epilogue to needy paths; after regalloc knows clobbers. (Per-register callee-save placement is a finer-grained variant of the same pass.) **NOT IMPLEMENTED — assessed 2026-08-29: sinking callee-save save/restore onto only the paths that need them requires machine-level liveness of each callee-saved register over the final CFG and save/restore placement at every entry/exit of that region, whereas `finalize_frame` is built around a single prologue/epilogue pair. A real frame-lowering change, not an allocator tweak; the cheap part of the row (save only the registers actually used) already ships as Callee-save selection.** |
| Y   | Y   | Instruction selection / combining              | 0 / 1 | codegen | Fuse MIR ops into machine instructions (e.g., FMA); MIR→machine lowering. (FMA fusion + adrp/add + cmp/branch fusion already exist.) **Base selection is mandatory lowering (not level-gated); only the optional cost-based *combining* is the dial pass.** **LANDED (plan-100): `fuse_scalar_fma` is NOT a dial row — it stays mandatory, ungated, in `src/codegen/compiler/opt/fma_fusion.rs`, alongside adrp/add + cmp/branch fusion. Contraction rounds once instead of twice, so it changes *which float values exist*: with `a = 1.5e308`, unfused `a * 2.0` rounds to `+inf` and `LET r = a * 2.0 - a` traps `ErrFloatOverflow`, while the fused `fmsub` yields a finite `1.5e308`. Two fixtures pin that as a contract (`rt-behavior/arithmetic/float-fma-fusion` + `rt-error/arithmetic/arithmetic-float-fma-observed-rt`), so gating it would make `-O0` silently change results — exactly what this table reserves Level 6 for, and `-O0` is a *safety* request. The dial row for this entry remains the future cost-based combining.** |
| Y   |     | Addressing mode optimization                   | 1 | codegen | Fold address math into addressing modes; machine-specific. |
| Y   |     | Bit-tricks / idiom recognition                 | 1 | Opt2 | Recognize popcount/bswap/rotate patterns; SSA pattern matching. |
| Y   |     | Division-by-constant lowering                  | 1 | Opt2 / codegen | Div → multiply-shift; MIR lowering or instruction selection. |
| Y   | Y   | Narrowing / bit-width reduction                | 2 | Opt2 | Shrink op widths when high bits unused; SSA demanded-bits analysis. **LANDED at Level 2 on the known-bits lattice (`opt2/knownbits.rs`): an `and` whose mask the value provably already satisfies is dropped for its source. **Measured: fires 0 today** — the corpus's masks all do real work.** |
| Y   | Y   | Sign/zero extension elimination                | 2 | Opt2 | Remove redundant extensions; SSA analysis. **LANDED at Level 2 on the known-bits lattice (`opt2/knownbits.rs`): a width mask or zero-amount shift over a value whose high bits are provably already clear becomes a copy. **Measured: fires 0 today** (same reason as the narrowing row).** |
| Y   |     | Select/cmov formation                          | 3 | Opt2 (late) | Turn diamonds into selects; pairs with if-conversion. **BLOCKED on the same missing primitive as the If-conversion row above (verified 2026-08-29): the neutral MIR has NO select op - `grep -n "Csel\|Cmov" src/arch/ops.rs` is empty, and the "csel/cmov are emittable" note on that row refers to the ISAs, not to this IR. Forming a select requires the new neutral op wired through `arch/ops.rs`, the `mir.rs` mirror lists, the effect model (a select reads two sources AND flags - new field roles) and regalloc, plus lowering in aarch64 (`csel`), x86-64 (`cmov`) and riscv64 (base RV64I has no conditional move, so a branchless expansion), and regenerated goldens on 5 targets. This row and If-conversion should land together or not at all.** |
| Y   | Y   | Dead global elimination                        | 2 | Opt1 | Remove unused globals/functions; module-level, before Plan1 symbols. **LANDED at Level 2 — `src/optimizer/opt1/globals.rs` on the shared whole-module census `opt1/plans/globals.rs`: a **private** global no function body and no other global's initializer mentions is removed. Exported globals are never touched (an importer may read or write them). **Measured: fires 0 today** — MFB programs barely have globals (examples/ai_chat has exactly one, and it is public).** |
| Y   | Y   | Merge duplicate constants/strings              | 2 | Plan1 / codegen | Deduplicate constant data; data layout concern. **ALREADY IMPLEMENTED (verified 2026-08-29, no new pass written): string constants are interned at collection — `push_string_value` (`data_objects.rs:1145`) refuses duplicates and `string_symbols` is a value→symbol map, so each distinct value yields exactly one `CodeDataObject`. Nothing reaches `layout_data_objects` to merge.** |
| Y   |     | Identical code folding                         | 4 | codegen / link | Merge identical function bodies; late, machine-code level. |
| Y   |     | Outlining                                      | 4 | Opt2 (late) / codegen | Extract repeated sequences into functions; post-SSA or machine level. |
| Y   |     | Link-time optimization (LTO)                   | 4 | Opt1 (whole-module) | Cross-unit optimization; run the Opt1 interprocedural passes over merged NIR. (Packages already merge into one NIR module — this is naturally whole-program.) |
| Y‡  |     | Profile-guided optimization (PGO)              | 4 | all stages | Profile data feeding Opt1 inlining, Opt2 unswitching, codegen layout. **‡profile-consumption is net-new; `--coverage` counter injection seeds the instrumentation side** |
| Y   |     | Hot/cold splitting                             | 3 | Opt2 (late) / codegen | Move cold paths away from hot code; late CFG/layout. |
| Y†  |     | Speculative hoisting                           | 6 | Opt2 | Execute instructions early; SSA + CFG dominance. **†off the dial: speculating a trapping op (overflow/index/div) above its guard changes behavior — Level 6 opt-in** |
| Y   | Y   | Partial redundancy elimination (PRE)           | 3 | Opt2 | Make partially redundant expressions fully redundant; SSA-based (often merged into GVN). **LANDED at Level 3 - `src/optimizer/opt2/pre.rs`. Completes an expression already computed on some paths into a join by computing it on the one path that lacked it, then deletes the join's own copy. Fires only when exactly ONE predecessor is missing it, so the insertion and the deletion cancel and the program never grows - a stronger guarantee than textbook PRE, chosen because without a profile there is no honest way to justify a net-growth insertion. Insertion is restricted to a predecessor whose only successor is the join (no critical-edge splitting, so the inserted work can never run more often than the deleted work did). **Measured: fires 0 on examples/ai_chat** - by the time PRE runs, value numbering and the memory rows have already taken the redundancy this corpus has.** |
| Y   | Y   | Load/store hoisting and sinking                | 3 | Opt2 | Move memory ops across branches; CFG + alias analysis. **LANDED at Level 3 (BOTH halves) - `src/optimizer/opt2/sink.rs`. *Sinking*: a stack-slot read moves down into the single branch that uses it, when that branch's only predecessor is the reading block (so it runs no more often than before) and nothing between the read and the branch may write memory. **Measured: 162 loads sunk on examples/ai_chat.** *Hoisting*: an identical `sp`-slot load or store leading BOTH arms of a branch moves up above it - both arms already ran it and the branching block runs exactly once per arm entry, so one copy runs the same number of times and one copy goes. It lands before the flag-setting run, never between a compare and the branch that reads it, and crosses only the terminator and that run, neither of which touches memory. A store may hoist where it may not sink, because hoisting does not change WHICH paths observe it.** |
| Y   | Y   | Bounds-check elimination                       | 3 | Opt2 | Remove provably-safe bounds checks; range analysis on SSA. (Real MFB checks raising `ErrIndexOutOfRange`; embryonic BCE already exists — `func_get.rs:152`, plan-86.) **~ Partial: plan-86 G1 `is_provable_index_access` / `provable_index_locals` (`builtins/collections/func_get.rs:152`) — embryonic, loop-index shapes only.** **LANDED at Level 3 - `src/optimizer/opt2/checks.rs` on the shared range lattice `opt2/plans/ranges.rs`, replacing the embryonic HIR-level shape matcher as the general answer (plan-86's `is_provable_index_access` still runs and is unchanged). An index test whose failing edge raises `ErrIndexOutOfRange` - identified structurally, by the error code the dead edge materializes before calling `_mfb_make_error_result`, not by a label spelling - is folded when the dominating conditions prove the index in range. **Measured: 7 on examples/ai_chat.** |
| N   |     | Null-check elimination                         | 3 | Opt2 | Remove redundant null checks; dominance-based. **MFB has no null model — see note.** |
| Y   | Y   | Correlated value propagation                   | 3 | Opt2 | Refine values using branch conditions; SSA + dominance. **LANDED at Level 3 - `opt2/checks.rs`. The range lattice refines each value from the compare-and-branch edges that dominate it; a comparison those facts settle becomes unconditional flow, and a value they pin to one number becomes that number. **Measured: 18 on examples/ai_chat.** |
| Y   |     | Fast-math transformations                      | 6 | Opt1 + Opt2 | Non-strict FP rewrites; algebraic ones in Opt1, contraction in Opt2/codegen. **Off the dial: MFB Float is strict/finiteness-trapped by default — Level 6 opt-in.** |
| N   |     | UB-based optimization                          | 6 | Opt2 | Exploit language UB rules (no signed overflow, etc.) in SSA simplification. **N/A: MFB has no undefined behavior; would be Level 6 if it did — see note.** |
| Y   | Y   | Alignment optimization                         | 2 | Plan1 / codegen | Align data (Plan1 slots) and loops/functions (codegen). **LANDED at Level 2 — `layout_data_objects` (`src/codegen/engine/types/types.rs`) orders each partition by descending alignment so every object lands already-aligned behind its predecessor, removing the padding a narrow-then-wide order wastes. Stable and symbol-tie-broken (deterministic blob); the two partitions sort independently so the const→writable page boundary is untouched. **Measured: 9 bytes saved on examples/ai_chat** — modest, because most objects are already 8-aligned.** |
| Y‡  |     | Prefetch insertion                             | 2 | Opt2 (late) | Insert prefetches for predictable streams; loop + stride analysis. **‡net-new prefetch encoders** |
| Y   |     | Structure field reordering / packing           | 4 | Opt1 (pre-Plan1) | Reorder fields for padding/locality; must precede Plan1 layout decisions. (Layout is not source-observable — no raw pointers — so this is always safe.) |
| Y   |     | AoS → SoA transformation                       | 4 | Opt1 (pre-Plan1) | Data layout transform; must precede storage planning. |
| Y   |     | Heap-to-stack promotion                        | 4 | Opt1 | Convert non-escaping heap allocs to stack; feeds Plan1 slot assignment (uses escape analysis). |
| Y   |     | Allocation sinking / elision                   | 4 | Opt1 + Opt2 | Eliminate allocations entirely; discovery in Opt1, final scalar promotion via Plan2/Opt2. |
| Y   |     | Tail merging (cross jumping)                   | 3 | Opt2 (post-SSA) / codegen | Merge identical branch tails; late CFG. |
| Y†  |     | Expression tree balancing                      | 6 | Opt2 | Balance dependency chains for ILP; SSA reassociation variant. **†off the dial: same trap/FP-order change as reassociation — Level 6 opt-in** |
| Y   |     | Polyhedral loop optimization                   | 5 | Opt1 | Advanced loop nest restructuring; needs structured loops, way easier pre-CFG. (Research-grade effort.) |
| Y   |     | Automatic parallelization                      | 5 | Opt1 | Thread-parallelize loops; structured-loop + dependence analysis. (Threading runtime exists; research-grade effort.) |
| Y   |     | Superoptimization                              | 5 | codegen | Exhaustive search for optimal machine sequences; final code level. (Applicable to any backend; research-grade.) |
| Y   | Y   | Code sinking                                   | 3 | Opt2 | Move a computation down into the branch that uses it, so cold paths don't pay; dual of hoisting. **†sinking a trapping op that removes a trap on the skip path is Level 6; the non-trapping/proven form is L3** **LANDED at Level 3 - `opt2/sink.rs`. A pure computation whose every use lies in blocks dominated by one successor moves into that successor, provided the successor's only predecessor is the current block - which makes the move unconditionally profitable with no trip-count guess, and is also why no loop analysis is needed (a sole-predecessor block cannot be a loop header). **Measured: fires 0 on examples/ai_chat**; the load half of the same mechanism fires 162 times.** |
| Y   | Y   | Aggressive DCE (ADCE)                          | 3 | Opt2 | Control-dependence-based DCE (assume dead, prove live); removes whole dead control structures plain DCE misses. **†removing a trap-capable region is Level 6; proof-gated form is L3** **LANDED: `src/optimizer/opt2/adce.rs` at Level 3 (`-O3`), the proof-gated form. `opt2/plans/postdom` computes postdominators (Cooper–Harvey–Kennedy over the allocator's CFG, virtual exit) + Ferrante-style control dependence; `plans/mark` runs the shared marking with conditional branches dead-until-proven — a dead branch is deletable outright because everything strictly between it and its ipdom is control-dependent (hence dead/swept) and falling through executes only labels. Trap gating is structural: a raise path's call/store seeds keep its guarding `b.vs`/`b.vc` live, so trap-capable regions are never touched; functions without postdominance facts (infinite loops) are skipped whole. Conservative leftovers: flag-defs (`cmp`) are always seeded live, so a fully-dead compare survives its deleted branch — a missed shrink, never a miscompile.** |
| Y   | Y   | Dead error-handler / fallible-branch elimination | 3 | Opt2 | When a fallible call is *proven* trap-free (range/trap analysis), its error branch and `RECOVER`/TRAP handling is dead. MFB-specific companion to check-elision and the union/error-tag analogue. Behavior-preserving. **LANDED at Level 3 - `opt2/checks.rs`. When the range facts prove a guard never fails, its raising edge is unreachable and the unreachable-code row sweeps the handler. The dead edge is identified structurally (it reaches `_mfb_make_error_result`), so this counts exactly the guards whose removal orphans a handler rather than ordinary control flow. **Measured: 19 on examples/ai_chat.** |
| Y   | Y   | CFG simplification (simplifycfg)               | 2 | Opt2 | Umbrella cleanup: block merge + branch fold + trivial-phi fold + two-entry-phi→select + empty-block removal + hoist common code from both arms. The named "rerun between passes" step. **LANDED at Level 2 — `src/optimizer/opt2/simplifycfg.rs`, the structural tidying the other CFG rows create and none of them does: a conditional branch whose two edges reach the same block (deleted — both ways go the same place), an unconditional jump to a return-only block (becomes the return), and duplicate consecutive labels (references retargeted at the first, the duplicate dropped). Runs after the other control-flow rows and before block merging. **Measured: 2646 rewrites on examples/ai_chat.** |
| Y   |     | Loop idiom recognition (broad)                 | 3 | Opt2 | Beyond memcpy/memset: popcount loops, strlen-style scans, arithmetic-series closed forms (`sum += i` → formula). **†closed-form replacement of a checked sum changes overflow behavior — Level 6 unless proven/versioned** **NOT IMPLEMENTED - assessed 2026-08-30. The row's own dagger note is the blocker: replacing a checked accumulation with a closed form changes which inputs raise `ErrOverflow` (`sum += i` over a large range traps partway; `n*(n+1)/2` traps at a different point or not at all), so the substitution is Level 6 unless run inside a guarded copy - the Loop versioning row, also not implemented. The non-arithmetic idioms (popcount loops, strlen-style scans) need a bulk primitive to lower to, which is the same missing neutral op that blocks the memcpy/memset row.** |
| Y   |     | Loop versioning                                | 3 | Opt2 | Emit two loop copies guarded by a runtime check (no-alias / in-range / trip-count); run the relaxed form only in the proven copy. **Behavior-preserving by construction — the mechanism that keeps vectorization and the † passes on the numeric dial instead of Level 6. Highest strategic value of the additions.** **NOT IMPLEMENTED - assessed 2026-08-30, and deliberately so: this row is infrastructure, not a win. Its entire value is letting some OTHER row run a relaxed form inside a guarded copy - vectorization, the dagger-marked numeric passes, the alias-scope row below. None of those is implemented, so emitting two copies of every eligible loop plus a runtime guard would double the loop's code size and buy nothing. It is the right first row to land when a consumer arrives, and the wrong one to land before. The nearest available consumer is not on that list, though, and is worth naming for whoever picks this up: the range lattice landed with the check-elision cluster. A loop versioned on a synthesized guard relating its bound to a collection's length would give the fast copy exactly the dominating fact the lattice needs to discharge every bounds check inside it - which is the classic win, and which the guard-less form cannot reach today. Duplicating a structured loop is already solved machinery (`opt1::unswitch` does it on a source condition); what is new is synthesizing the guard. **That chain does NOT connect today, and the break is worth recording rather than discovering again**: the guard's `len(xs)` and the bounds check's length are two separate reads of the collection's COUNT header word through a register base, and nothing landed unifies them - value numbering covers only the pure-ALU whitelist (`lvn::numberable_op` is `removable_op` minus the moves, and a load is not in `removable_op`), and redundant-load elimination is confined to `sp` slots. So the lattice sees two unrelated values and proves nothing, however good the guard is. The prerequisite is heap-load value numbering or an alias analysis, not the versioning itself.** |
| Y†  |     | Guard / check hoisting & combining             | 6 | Opt2 | Merge N per-iteration bounds/overflow checks into one dominating range check. **Off the dial by default: the combined check traps earlier, at a synthetic location, skipping loop-body effects — differs from MFB's per-expression source-stamped trap (§4.1 / §8.5a). Versioned (keep the strict copy) it drops to L3.** |
| Y   |     | Constant hoisting                              | 2 | Opt2 (late) / codegen | Materialize expensive constants (big immediates, address bases) once and reuse — matters on AArch64 (a 64-bit constant is up to 4 `mov`s). |
| Y   | Y   | Global localization / constification           | 2 | Opt1 | A global written once at init → constant; a global used by one function → local. Module-level; pairs with dead-global elimination. **LANDED at Level 2 (constification half) — `opt1/globals.rs`: reads of a never-written private global whose initializer is a literal are replaced by that literal, which then feeds the folding/propagation rows. A non-literal initializer is left alone (duplicating a call or allocation into every read would change how often it happens). The *localization* half — sinking a single-function global into that function — additionally needs a proof that its value never carries across calls, and is not attempted. **Measured: fires 0 today** (see the dead-global row: there are almost no private globals).** |
| Y   | Y   | Return-slot / NRVO copy elision                | 3 | Opt1 | Construct a value-semantic aggregate directly in the caller's return slot, eliding the copy. **Partially exists — `plan_returned_move` for `RETURN <owned-local>` (plan-25-C, `builder_exits.rs:258`); broadening beyond the direct-`RETURN`-local case is the net-new part.** **COMPLETE for every shape it is sound for (audited 2026-08-30 by reading the three deciding predicates, not from a mental model). `RETURN <owned-local>` moves the block (`plan_returned_move`, plan-25-C). `RETURN <fresh expression>` - a constructor, a `WITH`, an ordinary call - never copied in the first place: `value_needs_owning_copy` is false for it, so `lower_returned_value` returns the fresh block's pointer as-is. `RETURN <param>` elides through the plan-86 K1 param-borrow path, where the single deep copy lands at the caller's ownership boundary instead. What is left un-elided is exactly the set where eliding would be a use-after-free - a `by_ref` local, an address-taken local, a `FOR EACH` iterable local, and any local with no live `OwnedValue` cleanup at that slot - so this is not a gap to close. The Object / aggregate copy propagation row additionally widens the set of programs that REACH the direct case, by turning `LET b = a; RETURN b` into `RETURN a`.** |
| Y   | Y   | Live-range splitting                           | 2 | regalloc | Split a long live range so only part spills. The single highest-value linear-scan improvement; pairs with coalescing. **LANDED at Level 2 - `linear_scan.rs`: `first_free_until` + a `split_tail` map, with boundaries consumed in stream order emitting `move_register`/`float_move_d_from_d` and re-pointing `assignment`; an `every_home` snapshot feeds the callee-saved sweep. **Measured: fires 0 on examples/ai_chat** - the corpus's spilled ranges do not have a free register over part of their span. Correct and tested, not a win on this corpus.** |
| Y   | Y   | Machine copy propagation / redundant-move elimination | 1 | post-regalloc | Post-regalloc cleanup of moves coalescing/out-of-SSA left behind. Distinct from MIR-level copy propagation (`remove_fp_shuttles` peephole already does a slice of this). **LANDED (plan-100): `remove_fp_shuttles` is a gated Level-1 post-regalloc pass in `src/optimizer/opt2/` — on at `-O1`, off at `-O0`.** |
| Y   |     | Shared out-of-line trap stubs                  | 2 | codegen | Collapse each check's inline miss path (per-site park / build-Error-block sequence, ~40+ instrs — `emit_error_register_return`) into one `bl` to a shared per-error-kind stub. **Net-new: plan-16 already outlined the Error-*Result* assembly (`_mfb_make_error_result`), but each site still inlines the preamble/park/routing — highest value-per-effort given a check after nearly every checked op.** |
| Y   | Y   | Literal interning + read-only placement        | 2 | Plan1 / codegen | Dedup identical string/array literals and place immutable data in read-only pages. Companion to constant merging. **ALREADY IMPLEMENTED (verified 2026-08-29): interning is the same value→symbol map as the row above, and read-only placement is bug-187's existing partition — `layout_data_objects` emits every `kind == "constant"` object into a page-aligned rodata prefix ahead of the writable region.** |
| N   |     | Subregister-zeroing zext elimination (AArch64) | 2 | codegen | Exploit `w`-write-zeroes-upper-32 to drop explicit zero-extends. **N/A: the backend emits no `uxtw`/mask zero-extends — Integer math runs in 64-bit `x`-regs and the few narrow ops already rely on implicit zeroing. (Redundant *sign*-ext `sxtw` removal in FFI paths is the existing ext-elim row, still Y.)** |
| N   |     | Stack-protector / hardening omission           | 1 | codegen | Skip canary/CFI hardening on cold paths. **N/A: MFB emits no stack protector / canary / CFI / PAC / BTI today — nothing to omit. (The FFI OUT-buffer canary in `link_thunk.rs` is a correctness guard, not omittable.)** |
| Y   | Y   | Overflow-check elimination                     | 3 | Opt2 | Prove a checked integer op cannot overflow and drop its trap branch. **Exists (plan-39: `integer_sub_elidable`/`integer_add_elidable`, `builder_numeric.rs:750`); broaden with a real range analysis — likely MFB's single highest-value pass.** **~ Partial: plan-39 I1 — `integer_sub_elidable` (`local - positive-const` with a known lower bound) and the `local + 1` upper side (`builder_numeric.rs:787-812`). Two shapes, not a range analysis.** **LANDED at Level 3 - `opt2/checks.rs`, generalizing plan-39's two hand-matched shapes into a real range analysis. An `adds`/`subs` whose operand intervals cannot sum past the 64-bit range never sets `V`, so its `b.vc`/`b.vs` guard is decided. Trap discipline is structural: the lattice models a checked op's value ONLY when the true result is representable, so it can never claim to know the value of an operation that may raise. **Measured: 7 on examples/ai_chat.** |
| Y   | Y   | Redundant union-tag / error-tag check elimination | 3 | Opt2 | Remove a `MATCH` discriminant or fallible-result test dominated by an equivalent test. The direct MFB analogue of null-check elimination; preserve source-stamped traps. **LANDED at Level 3 - `opt2/checks.rs`: an equality/inequality test the range facts settle because a dominating equality already pinned the value. **Measured: fires 0 on examples/ai_chat** - the corpus's discriminant tests are not preceded by an equivalent dominating test.** |
| Y   | Y   | Division / modulo-check elimination            | 3 | Opt2 | Prove divisor ≠ 0 and (signed) exclude MIN ÷ -1; drop the corresponding error paths (`emit_integer_division_overflow_check`). **LANDED at Level 3 - `opt2/checks.rs`: the `x != MIN`, `y != -1` and `y != 0` guards that `emit_integer_division_overflow_check` and `emit_nonzero_or_invalid` emit, folded when the divisor's proven range excludes those values. Identified by the three label prefixes those two sites mint (`div_not_min`, `div_overflow_ok`, `nonzero`), each minted at exactly one site. **Measured: 32 on examples/ai_chat - the highest-firing check row.** |
| Y   |     | Float finiteness-check elimination & coalescing | 3 | Opt2 / codegen | Remove redundant observation-boundary finiteness checks or combine them — sanctioned by MFB's *explicitly imprecise* Float contract (§4.1). Distinct from fast-math. |
| Y   | Y   | Check fusion with existing comparisons         | 3 | Opt2 | Reuse a source/program comparison to discharge a bounds/overflow/divisor/tag/finiteness check instead of emitting another compare. **LANDED at Level 3 - `src/optimizer/opt2/flags.rs`. A forward walk tracks which comparison the condition flags currently reflect; a `cmp` that would re-establish an already-current one is deleted and its branch reads the earlier one's flags. Behavior-preserving with no value reasoning at all - only which comparison the flags describe. The transparency test is the new portable `plans::mark::flag_preserving`, NOT the pure-ALU whitelist: on x86-64 `and`/`orr`/`eor`/`add`/`sub` all write EFLAGS and `mov dst, xzr` is encoded as `xor dst, dst`, so treating those as transparent would read a comparison off flags some arithmetic had since overwritten. **Measured: 4 on examples/ai_chat.** |
| Y   | Y   | Range-check widening / narrowing               | 3 | Opt2 | Derive one safe-range fact from another (prove `i`,`i+1`,`i+2` from one dominating condition) without moving the trap. **LANDED at Level 3 - `opt2/plans/ranges.rs` pushes each block's dominating-condition facts forward through that block's own arithmetic, so one `i < n` also constrains `i + 1` and `i * 2`; `opt2/checks.rs` attributes a fold to this row when the deciding fact arrived that way rather than off the compare directly. No trap is moved - only facts are derived. **Measured: fires 0 on examples/ai_chat** (the check-bearing indices there are compared directly, not derived).** |
| Y   |     | Checked-operation fusion                       | 2 | Opt2 / codegen | Fuse a checked op + a compatible comparison/check into one flag-producing instruction, keeping the exact failing expression's source stamp. (Extends the existing `mir.rs` cmp/branch fusion.) |
| Y   |     | Trap-aware path-sensitive DCE                  | 3 | Opt2 | Model traps as explicit observable exits so cleanup is aggressive without treating a trap-capable op as dead/pure. **SUBSUMED - assessed 2026-08-30, no separate pass written. Both halves already hold. "Without treating a trap-capable op as dead/pure" is structural: `plans::mark::removable_op` excludes every flag-setter and every op that can raise, so no DCE row has ever been able to delete one, and ADCE additionally keeps a raise path's `b.vs`/`b.vc` guard live via its call/store seeds. "Model traps as observable exits so cleanup is aggressive" is what the unreachable-code row does with a terminal `FAIL`, now extended by the check-elision cluster: a guard the range facts prove always-failing makes its continuation unreachable and UCE removes it. What is left over would be a *third* notion of trap-as-exit with no rewrite attached to it.** |
| Y   | Y   | Error construction sinking                     | 3 | Opt2 | Keep `Error`/source-metadata/recovery-routing construction on the failing edge only; don't prepare it on the success path. **ALREADY IMPLEMENTED (verified 2026-08-30, no new pass written). Every trap site's error assembly - the code immediate, the line/column immediates, the message and filename symbol loads, and the `_mfb_make_error_result` call - is emitted by `raise_error_bare` INSIDE the raise block, i.e. on the failing edge only. Confirmed in a committed MIR golden (`tests/rt-behavior/control-flow/control-flow-if/golden/control_flow_if.macos-aarch64.mir:397-404`): the whole six-instruction construction sits after `add_ovf ... cond b.vc target overflow_ok_2`, which the success path branches over. There is no error/metadata preparation on a success path to sink. (`plan-64-I`'s `raw_result_discard_error` already skips even that assembly where the error is provably unused.)** |
| Y   |     | Error-path deduplication                       | 2 | Opt2 / codegen | Merge equivalent cold error blocks, keeping per-site source identity as a compact site-ID argument to a shared stub. (Pairs with Shared trap stubs.) |
| Y   | Y   | Recovery-region simplification                 | 3 | Opt2 | Coalesce nested `RECOVER`/TRAP regions, drop handlers that can't receive an error, route to the nearest live handler. **LANDED at Level 3 - `src/optimizer/opt1/recovery.rs`: a function-level `TRAP` whose guarded region provably cannot enter the error path is removed. Per spec 8.6 rule 3 the handler is reachable ONLY via `FAIL`, an auto-propagated call failure, or `FAIL`/`PROPAGATE` inside it - never by fall-through - so a region of pure value flow can never reach it. The proof is deliberately blunt (`dce::pure_non_trapping`): every call is fallible in MFB (8.1), all arithmetic is checked, `&` allocates, and a `RES` binding's lexical close can record a cleanup failure (the same `resource_close_function` test `module_may_record_cleanup_failure` uses). **The other two clauses of this row do not exist to do in MFB, verified 2026-08-30 by dumping the IR of both trap forms:** at most one function-level `TRAP` is legal per function (8.6 rule 1) so regions never nest and cannot be coalesced, and an inline `TRAP` is desugared away entirely before NIR - into `bind $trap_res`, `resultIsOk`, and an `IF` - so there is no handler to route to.** |
| Y   |     | Source-location metadata compression           | 2 | Plan1 / codegen | Dedup source-location records; pass compact site IDs to shared trap stubs — cuts the cost of precise traps *without* relaxing them. **BLOCKED — no metadata table exists to compress (verified 2026-08-29). An `ErrorLoc`'s filename is an already-interned string constant, and its line and column are emitted as *code immediates* into the error path (`error/emission/builder_error_emission.rs:152-160`, `move_immediate` into `c_arg(1)`/`c_arg(2)`). Compressing them would mean replacing those immediates with an index into a new side table — an error-path ABI plus runtime-format change, trading code size for a lookup during error construction, not an optimizer pass.** |
| Y   | ~   | Ownership-path optimization                    | 3 | Opt2 | Merge success/error cleanup paths, remove duplicate moves/drops, prove when an owned value need not be parked across a fallible op. **PARTIALLY LANDED, remainder needs a distinct row (assessed 2026-08-30 against a dumped MIR of a two-exit function holding an owned String). The "need not be parked across a fallible op" clause is exactly what the (landed) dead-store and partial dead-store rows do: an owned value spilled to a slot across a call, with the slot never read afterwards, is a dead store; read on only one edge, it is a partially dead one and sinks. The "merge success/error cleanup paths" clause is cross-jumping, and the MIR shows why it is not free here: the four exit paths' cleanup sequences are structurally identical but differ in vreg names (`%v76`/`%v77` vs `%v85`/`%v86`) and in per-site scratch slot offsets (288 vs 296), so a textual tail match catches only the trailing four loads and the `ret`. Merging the bodies needs cross-jumping WITH renaming - the Tail merging row below, which is codegen-paired.** |
| Y   |     | Destructor / drop optimization                 | 3 | Opt2 | Remove redundant drops, merge cleanup blocks, shorten ownership lifetimes — preserving deterministic destruction + trap order. (Machinery exists: `builder_owned_cleanup.rs`.) **NOT IMPLEMENTED - assessed 2026-08-30 with a dumped MIR. The "merge cleanup blocks" half is the same cross-jumping-with-renaming the Ownership-path row above measures as blocked: each exit path's `owned_value_free_skip_NN` guard, size computation, `_mfb_arena_free` call and null-out are emitted per exit with distinct vregs and distinct scratch slots. The alternative - emitting the scope-drop once into a shared epilogue block and branching to it - is a change to the `ActiveCleanup` emission model (`builder_owned_cleanup.rs`), which is where the drop lives; there is no drop node in NIR or MIR for an optimizer pass to move or delete. Same family as the Shrink wrapping row: a frame/cleanup-lowering change, not an optimizer one.** |
| Y   |     | Memory lifetime shortening                     | 3 | Opt2 | Move last-use cleanup earlier to cut peak memory + register pressure. **NOT IMPLEMENTED - assessed 2026-08-30. Drops are not represented anywhere the optimizer can see them: an owned binding's free is emitted by draining `self.active_cleanups` at each exit path (`builder_owned_cleanup.rs`), so "move the last-use cleanup earlier" means emitting the drop at a different point in the builder, not moving a node in NIR or MIR. Same emission-model blocker as the Destructor / drop row.** |
| N   |     | Reference-count operation elimination          | 3 | Opt1 + Opt2 | Cancel retain/release pairs, batch RC traffic. **N/A: MFB has no reference counting — compile-time ownership + arena (spec `memory-semantics`).** |
| N   |     | Write-barrier elimination / coalescing         | 3 | Opt2 | Remove/merge GC write barriers. **N/A: MFB has no tracing GC and emits no write barriers.** |
| Y   |     | Interprocedural / global DCE (call-graph)      | 4 | Opt1 | Call-graph reachability elimination of functions, methods, package initializers, unreachable SCCs. Distinct from the L2 dead-global row. |
| Y   |     | Global value specialization / global constant propagation | 4 | Opt1 | Propagate immutable/init-only globals across functions and specialize users. Broader than localization/constification. |
| Y   |     | Function merging (IR-level)                    | 4 | Opt1 | Merge semantically equivalent functions (incl. bodies differing only by constants or arg permutation) before codegen. More powerful than machine ICF. |
| Y   |     | Call-site splitting                            | 4 | Opt1 / Opt2 | Duplicate a join around a call so each copy gets stronger facts — enables specialization / const-prop / devirt. |
| Y   |     | Indirect-call promotion                        | 4 | Opt1 | Turn a profiled likely target (`FUNC`/closure call) into a guarded direct call + fallback. Complements devirt + PGO. |
| Y   |     | Recursive inlining / recursion peeling         | 5 | Opt1 | Inline/peel a bounded recursive layer to expose constants + base cases. Strict growth controls. |
| Y   |     | Hot/cold function outlining                    | 4 | Opt1 / Opt2 | Extract exceptional/rare paths (esp. bulky MFB trap/error construction) into cold functions. Broader than block placement + generic outlining. |
| Y   |     | Fallibility specialization                     | 4 | Opt1 | Clone a function into proven-infallible + general variants; the infallible clone uses a simpler return convention and omits error-tag handling. (MFB-specific.) |
| Y   |     | No-trap call specialization                    | 4 | Opt1 | Given inferred arg ranges, specialize a fallible callee into a `no-trap` version — unlocks LICM/DSE/tail-calls across it. |
| Y   |     | Result / tag representation specialization     | 4 | Opt1 | Specialize the internal fallible-result ABI when only one error kind is possible or payloads permit a cheaper representation. |
| Y   |     | Closure optimization                           | 4 | Opt1 | Elide non-escaping closure envs, drop unused captures, convert to direct calls, specialize by captured constants. **Partly exists: no-capture closures collapse to a static `env=0` descriptor; non-escaping capturing closures are drop-eliminated (`builder_control.rs:285`, plan-77).** |
| N   |     | Coroutine / state-machine optimization         | 4 | Opt1 | Dead-state removal, state merging, frame scalar-replacement. **N/A: MFB has no async/generators/resumable functions — concurrency is OS-thread workers only.** |
| Y   | Y   | Object / aggregate copy propagation            | 3 | Opt1 / Opt2 | Forward whole value-semantic aggregates + eliminate redundant copies not caught by scalar copy-prop or NRVO. **LANDED at Level 3 - `src/optimizer/opt1/aggcopy.rs`. `LET b = a` on a record, union, String or collection is a real deep copy of the block (value semantics), not a register move, so scalar copy propagation cannot reach it and NRVO only covers the `RETURN` position. The row deletes the binding and points `b`'s readers at `a` when BOTH names are immutable, never assigned, never address-taken (`LocalRef` is the one way a callee can change a local), never captured by a closure, and neither is a resource owner - and the source is a local binding rather than a parameter, whose block belongs to the caller. One block then does the work of two and one cleanup frees it. This also broadens the NRVO row for free: `LET b = a; RETURN b` becomes `RETURN a`, which `plan_returned_move` already elides.** |
| Y   |     | Allocation combining                           | 4 | Opt1 / Opt2 | Merge several short-lived allocations into one region/object where lifetimes permit. Complements allocation elision/sinking. |
| Y   |     | Dead allocation elimination                    | 4 | Opt2 | Remove an allocation whose result + initialization are dead, even when generic DCE doesn't model allocation effects. |
| Y   | Y   | Partial dead-store elimination                 | 3 | Opt2 | Remove stores dead on *some* paths, often by sinking/splitting them. **LANDED at Level 3 - the second half of `src/optimizer/opt2/dse.rs`, on the dataflow the full row already computes: `dead_in[s]` per successor is exactly "is this store dead entering `s`?", so a store dead on one edge and live on the other is partially dead and sinks into the edge that needs it. Requires the live successor's only predecessor to be this block (no other path may lose the store), a memory-quiet tail, and no redefinition of the stored register. Size-neutral; the dead path stops writing. **Measured: 1 on examples/ai_chat.** |
| Y   | Y   | Store PRE / Load PRE                           | 3 | Opt2 | Memory PRE — distinct scope from scalar PRE (alias + trap constraints differ). **LANDED at Level 3 - `opt2/plans/memory.rs` + `opt2/stldfwd.rs`, reading the per-predecessor exit states the availability fixpoint already computes (no second traversal). A load at the top of a join whose slot is available in the same single-def register on every incoming edge but one: the load is placed at the end of that one predecessor and the join's own load becomes a copy, which copy propagation and DCE then remove. The gap predecessor must reach the join unconditionally. **Measured: 3 on examples/ai_chat.** |
| Y   |     | Store merging                                  | 2 | Opt2 / codegen | Combine neighboring stores into wider stores / memset-like ops. |
| Y   |     | Load widening                                  | 2 | Opt2 / codegen | One wider load for adjacent fields when legal + safe at object/page boundaries. |
| Y   | Y   | Read-only memory inference                     | 2 | Opt1 | Infer immutable objects/regions and exploit that fact across calls. **LANDED at Level 2 — `opt1/globals.rs` clears `mutable` on a private global the census proves is never written, so Plan1 storage may place it read-only (the placement itself is bug-187's existing `constant`-kind rodata partition). **Measured: fires 0 today** (same corpus reason).** |
| Y   |     | Alias-scope specialization via versioning      | 3 | Opt2 | No-alias guarded fast path + strict fallback — a use of the Loop-versioning machinery. **NOT IMPLEMENTED - assessed 2026-08-30. Explicitly "a use of the Loop-versioning machinery", which is not implemented (see above), and it additionally needs the alias analysis this seam still lacks - every memory row here is confined to `sp` slots precisely because nothing yet distinguishes two heap addresses. Two missing prerequisites, neither of which this row can supply.** |
| Y   |     | Aggregate load/store combining                 | 2 | Opt2 / codegen | Combine adjacent scalar accesses into pair/vector accesses (AArch64 `ldp`/`stp`) when alignment + trap behavior permit. |
| Y   | ~   | Stack allocation merging / frame compaction    | 2 | regalloc / codegen | Coalesce fixed stack objects (incl. non-spill locals) with non-overlapping lifetimes. Broader than spill-slot coloring. **PARTIALLY LANDED, remainder BLOCKED (verified 2026-08-29): the *spill* area is now compacted by the Stack slot coloring row (values with disjoint live ranges share a slot). The remainder is merging Plan1-assigned **locals**, which needs local-lifetime analysis plus an address-taken (escape) proof — `NirValue::LocalRef` hands out a slot pointer, so two locals cannot be overlapped without knowing neither escapes.** |
| Y   | Y   | Prologue / epilogue optimization               | 2 | regalloc / codegen | Fold stack adjustment, paired saves/restores, return forms, leaf-function cases. Related to shrink-wrapping, distinct. **LARGELY ALREADY IMPLEMENTED (verified 2026-08-29): `finalize_frame` (`engine/util/vreg_frame.rs:80`) returns a zero-size frame with no prologue/epilogue at all when nothing needs stack; only callee-saved registers the coloring actually used are saved (the Callee-save selection row); and the link register is saved only when the function really contains a call. What remains of this row is shrink wrapping, which is its own row below.** |
| Y   | Y   | Callee-save selection                          | 2 | regalloc | Cost-based caller-saved-spill vs callee-saved-occupancy choice. Important for linear scan + hot loops. **IMPLEMENTED: only callee-saves the coloring actually used are spilled (`used_callee_saved`, `builder_registers.rs:183-237`) — the per-register form of this row. Shrink-wrapping (sinking them to needy paths) is the net-new part.** |
| Y   | Y   | Spill-code optimization                        | 2 | regalloc / post-regalloc | Fold reloads/spills into memory operands, kill redundant reloads, place spills optimally. **LANDED at Level 2 — `linear_scan.rs`: the rewrite emits a reload before every use of a spilled value independently, so a reload whose value is already resident in the target register (just stored there, or loaded there with no intervening write to register or slot) is redundant and deleted. Any unmodeled instruction ends the window. **Measured: 62042 reloads removed on examples/ai_chat.** |
| Y   |     | Register-pressure-aware code motion            | 3 | Opt2 / scheduling | Constrain LICM/sinking/CSE/scheduling by estimated pressure so "optimized" MIR doesn't over-spill. |
| Y   | Y   | Machine branch relaxation                      | 0 | codegen | Select short/long branches, insert veneers/islands when range overflows. **Backend facility (correctness-required) + net-new: today AArch64 *hard-errors* on out-of-range branches (`sizing.rs:86`, the >1 MiB failure) — this pass removes that failure class.** **IMPLEMENTED: AArch64 conditional-branch veneer insertion (`src/arch/aarch64/encode/relax.rs`, bug-445) — a far `b.<cond>` becomes a short conditional to a trampoline holding an unconditional `b`. Mandatory, not a dial row: without it a >±1 MiB function fails codegen outright.** |
| Y   |     | Constant-island / literal-pool placement       | 1 | codegen | Place + dedup literals within architectural reach; coordinate with layout + branch relaxation. Backend facility. |
| Y   |     | Machine block reordering + fall-through inversion | 2 | codegen | Invert conditions + reorder successors to maximize fall-through (concrete form of the branch-layout-hints row). |
| Y   |     | Machine tail-call formation                    | 4 | post-regalloc / codegen | Catch ABI-valid tail calls exposed only after lowering/frame layout/copy resolution. Complements MIR-level TCO. |
| Y   |     | Load/store pair formation (ldp/stp)            | 1 | codegen | Form AArch64 `ldp`/`stp` for adjacent accesses + saves/restores. **Net-new: backend emits only single `ldr`/`str` today.** |
| Y   |     | Pre/post-index addressing formation            | 1 | codegen | Fold pointer updates into AArch64 memory ops when ordering is preserved. **Net-new: no writeback addressing today.** |
| Y   | ~   | Compare elimination / flag reuse               | 1 | Opt2 / codegen | Reuse NZCV from arithmetic/prior compares; avoid repeated `cmp`/`test`. **PARTIALLY LANDED - `src/optimizer/opt2/flags.rs` (landed as the Check-fusion row below, but it is the same mechanism this row names). A forward walk over the CFG tracks which comparison the flags currently reflect; a `cmp` that would re-establish an already-current one is deleted and its branch reads the earlier one's flags. That is the "general flag reuse" half this row called net-new. Still missing: reuse of NZCV set by ARITHMETIC (an `adds`/`subs` whose flags a later branch could read) - only prior `cmp`/`cmp_imm` are tracked. **Note a level mismatch worth a decision: this row is classified Level 1, but the implementation is gated at Level 3, so it does not run at the default `-O1`.** It could be dropped to Level 1 - it is behavior-preserving with no value reasoning at all - but that would move default-level codegen and churn the byte-identity goldens, so it was left at 3 rather than decided unilaterally.** |
| Y   |     | Boolean materialization elimination            | 1 | Opt2 / codegen | Feed flags straight into branches/selects instead of producing + retesting a Boolean register. |
| Y   | Y   | Known-bits simplification                      | 2 | Opt2 | Track known-0/1 bits to simplify masks/shifts/compares/extensions/alignment checks. Broader than demanded-bits narrowing. **LANDED at Level 2 — `src/optimizer/opt2/plans/bits.rs` (the known-bits lattice: per-value `ones`/`zeros` masks refined by a worklist fixpoint over the SSA overlay, with transfer functions only for the pure flag-free whitelist, so checked arithmetic is structurally outside it) + `src/optimizer/opt2/knownbits.rs`. Rewrites a fully-known result to `mov_imm` and a provably-no-op operation (`x ORR 0`, `x EOR 0`) to a copy. **Measured: fires 0 on the current corpus** — the bit-op-heavy fixture has 24 `and`s and 22 shifts but none is redundant, so declining is correct behavior, not a broken row.** |
| Y   |     | Bit-field extract/insert recognition           | 1 | Opt2 / codegen | Recognize mask/shift patterns as `ubfx`/`sbfx`/`bfi`. |
| Y   |     | Carry / borrow chain formation                 | 1 | Opt2 / codegen | Form adc/sbc sequences for wide arithmetic or checked-op idioms. |
| Y   | Y   | Multi-instruction constant synthesis           | 1 | codegen | Cost-model choice among `movz`/`movk`/`movn`, literal loads, logical immediates, shared bases. **IMPLEMENTED: `movz` for the low halfword plus `movk` only for nonzero higher halfwords (`aarch64/encode/emitter.rs:601`), i.e. the minimal-instruction form this row asks for.** |
| Y   |     | Reciprocal / remainder lowering (exact)        | 1 | Opt2 / codegen | Expand exact constant division + modulo jointly, sharing the quotient + checks. |
| Y   |     | Loop rerolling                                 | 3 | Opt2 | Recognize unrolled repeated bodies and reconstruct a loop (shrinks size, re-enables vectorization). **NOT IMPLEMENTED - assessed 2026-08-30. Rerolling reconstructs a loop from replicated bodies, and the pipeline produces none: Loop unrolling (Level 5, row above) is not implemented, so the only possible input is hand-unrolled source. Recognizing that is a substantial body-equivalence matcher for a win this corpus cannot measure - and its stated purpose, re-enabling vectorization, depends on a vectorizer that is likewise not implemented.** |
| Y†  |     | Loop predication                               | 5 | Opt2 | Convert per-iteration conditions/checks to predicates. **†strict trap semantics need proof or versioning — dial-safe form is versioned, else Level 6.** |
| Y   |     | Loop flattening / collapse                     | 3 | Opt1 | Collapse nested iteration spaces where ordering + overflow are preserved. **†checked index arithmetic needs proof.** **NOT IMPLEMENTED - assessed 2026-08-30, a pessimization in MFB. Flattening pays when the body addresses a linearized index (`a[i*m + j]`), so the fused counter replaces the pair directly. MFB has no such indexing - collection access is `get(xs, idx)` - so both original indices have to be RECONSTRUCTED in the fused body as `k / n2` and `k MOD n2`, a division and a modulo per iteration in place of the one increment and one compare the inner loop control cost. On top of that the fused bound `n1 * n2 - 1` is a checked multiply that can raise where the nested form could not, so it would need a non-overflow proof to stay behavior-preserving.** |
| Y   |     | Loop reversal                                  | 3 | Opt1 | Iterate in reverse to improve addressing / cut induction work. **†must preserve operation/trap order where observable.** **NOT IMPLEMENTED - assessed 2026-08-30. The row's own note requires preserving operation and trap order "where observable", and in MFB a loop body's order is observable by default: bodies print, call, and mutate, and every call is a potential trap site whose order relative to the others is part of the program's behavior. Reversal is therefore sound only for a body with no observable effects and no cross-iteration dependence - which is a body the dead-code rows already delete. The payoff (comparing a counter against zero instead of a bound) is a codegen-level micro-win that does not justify the proof obligation.** |
| Y   |     | Loop-invariant branch elimination              | 3 | Opt2 | Delete/fold an invariant branch without duplicating the whole loop (lighter than unswitching). **SUBSUMED for the decidable half; the rest is unsound without duplication (assessed 2026-08-30). A conditional branch in a loop whose outcome the dominating facts settle is already folded by the range-driven cluster (`opt2/checks.rs`) - loop membership is not an extra ingredient, and invariance is not what makes it foldable. What remains is deleting a branch whose outcome is the same every iteration but UNKNOWN, and there is no sound way to delete a branch without knowing which way it goes; making it known requires emitting both forms under a test, which is exactly the (landed) Loop unswitching row.** |
| Y   |     | Loop exit-value simplification                 | 3 | Opt2 | Compute post-loop induction values directly; remove exit phis / final-iteration work. **†checked arithmetic needs proof.** **NOT IMPLEMENTED - assessed 2026-08-30. A `FOR` counter is scoped to its loop, so the classic post-loop induction read does not exist in MFB source; the reachable case is a `MUT i` declared outside a `WHILE`, where replacing a post-loop read of `i` with the bound saves nothing at runtime (the value is already in a slot). The form that DOES pay - deleting the loop and computing its result in closed form - is the Loop idiom recognition row below, whose closed-form substitution of a CHECKED sum changes overflow behavior and is therefore Level 6 unless run under the Loop versioning row's guard.** |
| Y   | Y   | Loop-nest invariant code motion                | 3 | Opt2 | Hoist to the shallowest safe loop level, not merely out of one loop. **LANDED at Level 3 - `src/optimizer/opt2/licm.rs` on the new `opt2/plans/mirloops.rs` (natural loops from back edges + the SSA overlay's dominators, so it sees the loops the MACHINE has - the collection, string and desugared ones no NIR pass can). Walks the nest outward re-testing invariance, so a value invariant at two levels lands in the outer preheader rather than merely outside the inner loop. Only pure whitelist ops with a single-def `dst`; a trapping op is never hoisted (it would raise in front of a loop that might run zero times). Never synthesizes a preheader. **Measured: fires 0 on examples/ai_chat.** |
| Y   |     | Unroll-and-jam                                 | 5 | Opt1 | Unroll an outer loop + fuse inner bodies for reuse/vectorization. Distinct from plain unroll + fusion. |
| Y   |     | Strip mining                                   | 3 | Opt1 | Partition the iteration space explicitly; foundation for tiling + vector-width loops. **NOT IMPLEMENTED - assessed 2026-08-30. Strip mining is order-preserving and easy, and on its own it is pure loss: it replaces one loop's control with two nested loops' control and grows the code, buying nothing until a consumer uses the partitioned iteration space. Its consumers are tiling and vector-width loops, both Level-5 rows that are not implemented, so landing it now would make every affected loop slower and larger with no offsetting row to collect the benefit.** |
| Y   |     | Vectorization epilogue optimization            | 5 | Opt2 | Masked tails / scalar epilogues / epilogue vectorization / multiple vector widths. |
| Y†  |     | Reduction recognition + vectorization          | 5 | Opt2 | Sums/products/min-max/logical reductions. **†checked integer reductions need proof/versioning or Level-6 relaxation.** |
| Y   | Y   | String concat / rope fusion                    | 3 | Opt1 / Opt2 | Fuse a chain `a & b & c` into one pre-sized allocation + writes instead of an intermediate per operator. **Real gap: general `&` chains allocate + discard intermediates (`lower_string_concat`, `builder_value_semantics.rs:475`); only the self-append idiom `s = s & …` is already fused (plan-02 §4.1). Highest-value string win.** **LANDED at Level 3 - `lower_string_concat_chain` (`builder_value_semantics.rs`), dispatched from `builder_values.rs` after flattening the left-leaning `&` spine. `&` is left-associative, so `a & b & c` arrived as `(a & b) & c` and the pairwise lowering allocated and filled a whole intermediate for `a & b` only to copy it again - n-1 allocations and quadratic copying for an n-operand chain. The fused form sums the header lengths, allocates once, and copies each byte once. Operands are still lowered left to right, so a failing one fails at the same point; the only difference is that the intermediate allocations never happen, and an arena block nothing can observe is not observable. Two-operand `a & b` still goes through the pairwise path verbatim, and the row is Level-3-gated, so the default artifact gate is untouched. |
| Y   | ~   | Small-string / small-array optimization (SSO)  | 4 | Opt1 (pre-Plan1) | Inline storage for short strings/arrays to skip a heap/arena allocation; a representation decision before Plan1. **Real gap: general non-empty String/List/Array always arena-allocate; only empty-string, static literals, and the fixed `FloatN` vector carrier are inline today.** |
| Y   |     | Multi-value return in registers                | 4 | Opt1 | Return a small record in value registers instead of boxing it and returning a pointer. **Real gap: records are materialized in the arena and returned as one pointer (`materialize_inline_value_in_arena`, `builder_exits.rs:335`); the 4-register form is the error-outcome ABI, not aggregate decomposition.** |
| Y   |     | Union layout narrowing                         | 4 | Opt1 | Drop the tag word from a union's runtime representation when it is provably single-variant / statically known. **Real gap: the tag is always stored at offset 0 (`store_u64(tag, block, 0)`); distinct from the tag-*check* elimination row (that removes tests, not the field).** |
| Y   |     | Error-payload per-call-site specialization     | 4 | Opt1 / Opt2 | When a call site only checks *whether* it failed (not *which* error), skip constructing the `Error` payload and just set the fail flag. Call-site-specific — pairs with, but distinct from, per-function Result/tag specialization and per-edge error-construction sinking. |
| Y   | Y   | Codepoint `len()` caching                      | 3 | Opt1 | Cache the scanned UTF-8 codepoint count of a String across uses. **Byte length is already an O(1) header read (`strings.byteLen`), but `len(str)` scans codepoints every call (`builder_collection_layout.rs:1179`) — the scan result is the cacheable quantity.** **LANDED at Level 3 - `src/optimizer/opt1/lencache.rs`, on NIR rather than MIR (stage corrected: `len` is still a recognizable call there, and by the Opt2 seam it has become the flat byte loop). Repeated `len(s)` on the same String local within one straight-line run bind the first scan and read it. Restricted to `String` on purpose: for a collection `len` is already the O(1) header count, so there is nothing to cache and caching could go stale. A run ends BEFORE any restructuring statement and before any write to the cached name - the barrier's own values never join it, because a `WHILE` condition is re-evaluated every iteration and its BODY can assign the String, an assignment this scan cannot see. Folding them in hung `rt-behavior/native/native-cbuffer-read-rt` at `-O3`; the regression test pins the shape. **Measured: fires 0 on examples/ai_chat and examples/browser/app** - after that fix the corpus has no qualifying run - but verified firing and correct end to end on a probe.** |
| Y   |     | Multi-way select / CSEL-chain formation        | 2 | codegen | Build 3+-way `csel`/`csinc` (AArch64) or `cmov` (x86) chains where a later select's false input is a prior select's result. Distinct from single-select formation. |
| Y   |     | x86 LEA-as-arithmetic                          | 1 | codegen | Use `lea` as a 3-operand add / shift-add for *arithmetic* (not just address computation). Distinct from the addressing-mode row. |
| Y   |     | Compare-with-zero branch (cbz/cbnz) formation  | 1 | codegen | Fold `cmp x, #0` + `b.eq`/`b.ne` into AArch64 `cbz`/`cbnz`. Small peephole complementing the existing cmp/branch fusion. |
| Y   |     | SoA → AoS transformation                       | 4 | Opt1 (pre-Plan1) | The reverse of AoS→SoA — choose array-of-structs for pointer-chasing-light access patterns. Completes the bidirectional layout choice (pick a direction by access pattern). |

## Feasibility notes (grounded in the current compiler)

### The two N's

- **UB-based optimization — N.** MFB has *no undefined behavior* to exploit. Integer arithmetic is **checked and traps `ErrOverflow`** (never wraps) — `mfb spec language types` §4.1; codegen emits explicit overflow checks (`builder_numeric.rs:854/888/949`, `emit_overflow_if_flags_set` at `:1165`). There are no raw pointers (memory-safe by construction), and Float is finiteness-trapped (`ErrFloatNaN`/`ErrFloatOverflow`), not UB. The classic C levers (assume-no-signed-overflow, strict aliasing, assume-no-null-deref) simply don't exist. The realistic analogue is the *opposite* — **proving checks safe and eliding them** — which already exists (overflow-check elision, plan-39; BCE, plan-86). Pursue more provable elision, not UB assumptions.

- **Null-check elimination — N (as written).** MFB has no null/absent form for values: fields are mandatory owned values (§4.2), there is no built-in Option/Maybe (absence is an `error(...)`, §4.4), and `Nothing` is a unit marker (§4.6). So there are literally no null checks in emitted code to remove. **The valuable analogue is a distinct pass worth adding: redundant union-tag / error-tag (fallible-call) check elimination** — dominance-based removal of a `MATCH` discriminant test or a fallible-call success/error branch already proven on a dominating path. That analogue is a clean **Y** and belongs in Opt2; it's just not "null-check elimination."

### The cross-cutting constraint that colors every † row: checked-overflow trapping

Because integer `+ - * ^` and `MOD` **trap deterministically on overflow**, and index/division ops trap too, any transform that **reorders, removes, speculates, or re-associates** a trapping operation can change *whether/when a program raises* — which is observable behavior, not UB. Concretely, the † rows must gate on trap-safety:

- **Reassociation / expression-tree balancing / loop strength reduction:** `(a+b)+c` and `a+(b+c)` can differ in *which* addition overflows. Safe only on FP (which is order-defined but not trapping mid-expression), on values *proven* not to overflow (range analysis), or on operations already elided to unchecked. Do **not** freely reassociate checked integer arithmetic.
- **LICM / speculative hoisting:** hoisting a checked op above its guard can make it trap on a path (or iteration count) where the original never executed it. Hoist only trap-free ops, or ops proven in-range at the hoist point.
- **Loop deletion:** a loop whose body can trap (overflow, `ErrIndexOutOfRange`) is **not** side-effect-free — deleting it can remove an observable raise. Delete only after proving the body is trap-free.

This is why these passes were pulled onto **Level 6** (opt-in, off the numeric dial): their unproven form is a semantic relaxation. Their *proof-gated* safe form stays on the dial (LICM at L3 hoisting only trap-free ops; deleting a proven-trap-free loop at L2). Either way, the enabler is a **range/trap analysis** — now a **Plan2 prerequisite** (demand-driven, not a dial row — see plan-100), not an optional extra.

### The ‡ rows: applicable, but need net-new infrastructure

- **Auto-vectorization / SLP — ‡.** True packed SIMD **already exists and is emitted today**, but only 2-lane/128-bit (`FAddV`-family in `src/arch/ops.rs:144`, real NEON `.2d` at `aarch64/encode/emitter.rs:443`, SSE packed at `x86_64/encode/emitter.rs:974`, RISC-V scalarized fallback), driven only by `math::` array kernels (`builder_simd_math.rs`). A vectorizer would **reuse those encoders + `abi::vector_*` helpers** for f64×2 / i64×2 — no new encoders for the 2-lane case — but it's net-new *analysis* (dependence/trip-count + widening selection). Wider forms (4×f32, AVX ymm) *are* net-new encoders. Note: the `vector::` package is **not** packed SIMD — it keeps one scalar FP reg per lane (`builder_vector_inline.rs`).
- **Instruction scheduling / software pipelining — ‡.** No scheduler, latency model, or machine model exists today (searched `src/target`/`src/arch`: no `schedul|latency|pipelin|hazard`). Instructions emit in-order as lowered; the only post-lowering passes are FMA fusion + two peepholes. Feasible, but the machine model is entirely net-new.
- **PGO — ‡.** No profile-consuming optimizer today. But `mfb test --coverage` already injects per-statement block counters (`src/testing/desugar/coverage.rs`) and writes a `.covdata` sidecar — a working precedent for the *instrumentation* half. The *consumption* half (edge weights → inlining/layout/BCE) is net-new.
- **Prefetch insertion — ‡.** Needs prefetch instruction encoders (`prfm`/`prefetcht0`) that aren't present yet; addable.

### Free wins MFB's semantics hand you (not in the classic C playbook)

Memory-safety + checked arithmetic mean the highest-value early passes are **check-elision** ones: broaden the existing **overflow-check elision** (plan-39, `elide_overflow`/`integer_sub_elidable`) and **bounds-check elimination** (plan-86, `is_provable_index_access`) with a real **range/value-range analysis** (a Plan2 prerequisite — see plan-100). These remove real, measurable per-operation checks the compiler emits today — a bigger and safer payoff than chasing UB-style transforms that don't apply here. Note these are behavior-*preserving* (they only elide a check when they prove it can never fire), so they stay on the numeric dial — unlike the Level-6 relaxations.

### Plan2 infrastructure, not dial rows

Several items that look like passes are really the *fact base* other passes consume — like alias/range analysis, they belong in plan-100's Plan2 as demand-driven infrastructure, gated by "does an enabled pass need this," not by a level number:

- **Function attribute inference** (`pure` / `readonly` / `noreturn` / **`no-trap`**). Bottom-up per-function facts. The `no-trap` attribute is load-bearing: without it, every call is an optimization barrier under MFB's semantics (any callee might raise), so LICM/DSE/sinking *across calls* all depend on it. High priority — but as a prerequisite, not a numeric-dial row.
- **Trivial-phi / phi simplification.** SSA *maintenance* (remove trivial/duplicate phis), runs whenever SSA is live as part of Plan2 construction + out-of-SSA. Constantly re-runnable, never level-gated. Fold into Plan2, not a row. (The *user-visible* CFG cleanup — two-entry-phi→select, empty-block removal — is the separate "CFG simplification" dial row above.)
- **Memory SSA / memory-dependence analysis.** The shared representation that gives DSE, load elimination, store-to-load forwarding, memory PRE, and LICM their memory facts. Infrastructure, built alongside Plan2's alias analysis — not a dial row.
- **Loop canonicalization.** Preheaders, dedicated exits, latch normalization, reducible-loop normalization. Enabling infrastructure for every loop pass; runs whenever any loop pass is enabled, not level-gated.
- **Register allocation & base instruction selection** are also infrastructure (they run at every level — marked `—` in the Level column); only their *refinements/combining* are dial passes.

### The trap-precision decision (verified against the spec)

The one spec-level fork the review flags is real and only partly pre-decided (`mfb spec language types` §4.1, error-model §8.1/§8.5a):

- **Integer/Byte:** checked **per operation**, never wrap; `Error.source` is stamped at the *failing expression's* location and never rewritten. So the spec already pins expression-level precision — you cannot silently move *where* an integer trap claims to originate. That is what makes "Guard/check combining" a Level-6 (behavior-changing) pass by default, and what keeps the trap-order † rows †.
- **Float:** *explicitly imprecise* — finiteness is enforced "at observation boundaries rather than after each operation," transient non-finite intermediates are allowed to recover without trapping. So combining/reordering **Float** finiteness checks is already sanctioned by the spec (those rows are behavior-preserving, dial-eligible).
- **Not addressed by any named guarantee:** per-*iteration* precision and whether equivalent integer checks may be reordered/combined. This is the deliberate decision to pin before building the range/trap analysis. Two coherent answers: **(a)** keep precise integer traps and buy performance with **loop versioning** (recommended — preserves MFB's deterministic-error value proposition); or **(b)** weaken the integer contract to "same error for the same input, iteration/location unspecified," which drops the † from check-combining/early-trapping and several loop rows — at the cost of user-visible determinism. They are alternatives, not a stack.

### Reclassifications flagged by review (existing rows)

Level tweaks the review is right about — the loop ones are edited in the table; the rest are constraints on *when* a row keeps its level:

- **Loop interchange / tiling / skewing: 3 → 5** (edited). They rely on array dependence analysis, which is the "deepest-analysis, silent-corruption" risk that defines L5 — even though results are preserved. **Loop fusion / fission stay L3** (coarser legality check, no full dependence solver).
- **Register allocation & instruction selection → infrastructure** (marked `—`), not dial rows — they run at every level; only refinements/combining are gated.
- **Constant folding is L1 only for non-trapping expressions.** Folding a *trapping* constant expression (e.g. a constant overflow or divide-by-zero) must preserve the same runtime trap, source location, and execution condition — turning a conditionally-executed runtime trap into an unconditional/earlier one is a behavior change (L6, or emit a compile-time diagnostic only when the expression is unconditionally evaluated).
- **Division-by-constant lowering is L1 only for the arithmetic;** the checked path (zero divisor, signed MIN ÷ -1) must still be handled correctly — the row assumes the strength-reduced sequence preserves those traps.
- **Prefetch stays L2, deliberately:** on both arches the prefetch instruction is architecturally non-faulting (AArch64 `prfm`, x86 `prefetch*`), so it cannot miscompile — correctness risk is ~zero, which is an L2/L1 trait, not L3. It *is* code-additive; that's a code-size/perf concern, not a risk-level one.
- **Jump-table generation / switch lowering** are behavior-preserving codegen *policy* (not "same operations executed"); L1 reflects their ~zero miscompile risk, but read them as lowering-strategy choices rather than transparent rewrites.

### Proposed but moot / already done (recorded so they aren't re-proposed)

Verified against the source — these are *not* gaps:

- **Arena / thread-local state register promotion — already done.** The arena base *and* current-thread pointer are program-wide pinned, allocation-reserved registers on every ISA (AArch64 `x19`/`x20`, x86-64 `r15`/`rbx`, riscv64 `s11`/`s2`; `abi.rs:179`, `:227`). Nothing to promote.
- **String *byte*-length caching — moot.** Byte length is an O(1) header field at offset 0 (`strings.byteLen` = one load). (Only the *codepoint* `len()` scan is cacheable — that's a real row above.)
- **Load-and-zero-extend fusion — N/A.** The backend emits no explicit zero-extensions (Integer math runs in 64-bit `x`-regs; narrow ops rely on implicit `w`-write zeroing) — this is the AArch64 subreg-zext N row.
- **Write-combining buffer optimization — N/A.** MFB has no MMIO / write-combining memory model.
- **Lock-free mailbox pattern recognition — deferred.** Message-passing goes through runtime primitives; recognizing and rewriting synchronization is research-grade, not a near-term row.

**Key architectural takeaways for your pipeline:**

1. **Opt1's job**: interprocedural transforms (inlining, specialization, dead args), anything that **changes storage layout or signatures** (SROA, field reordering, escape analysis) — these *must* land before Plan1, and structured-loop transforms (interchange, tiling, fusion) which are much harder once you're on a CFG.
2. **Opt2's job**: everything requiring dataflow — GVN, SCCP, LICM, PRE, DSE, alias-based passes, vectorization, and late CFG cleanup (if-conversion, tail merging) just before out-of-SSA.
3. **Ordering within Opt2**: build SSA → SCCP → GVN/CSE → LICM/loop passes → alias-based memory passes → vectorize → late CFG cleanup → out-of-SSA. Rerun DCE + folding between major passes.
4. **Out-of-SSA copies**: register coalescing during regalloc is what cleans up the copies out-of-SSA inserts — treat them as one design problem.
5. **Watch for**: Opt1 inlining creating huge functions that blow up Opt2's SSA analyses — put an inline budget in early. (And beware the AArch64 >~1 MiB single-function branch-range limit — over-inlining can make a function uncompilable.)
6. **Trap-safety gate**: build the "can this op trap?" predicate + range analysis as a **Plan2 prerequisite** (plan-100), demand-driven. LICM stays on the numeric dial (L3) but may hoist only ops it proves trap-free; the trap-order-*relaxing* passes (reassociation, expression-tree balancing, loop strength reduction, loop deletion, speculative hoisting) plus fast-math live at **Level 6** — opt-in, never implied by the dial.
7. **Two-axis control**: the numeric dial (`-O0..-O5`) escalates *shape distortion at preserved behavior* and gates each row by `row.level <= active_opt_level()` in **both** seams; **Level 6** (`-O6`) is the orthogonal semantic-relaxation opt-in. Prerequisites (SSA/mem2reg, alias, range/trap analysis) are demand-driven Plan2 infrastructure, not levels.
