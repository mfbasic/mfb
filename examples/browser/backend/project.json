{
  "name": "backend",
  "version": "0.1.0",
  "mfb": "1.0",
  "kind": "package",
  "description": "HTTP fetch worker for the browser example: runs the blocking load (with redirect following) on its own thread so the UI stays responsive.",
  "sources": [
    {
      "root": "src",
      "role": "package",
      "include": ["**/*.mfb"]
    }
  ],
  "config": {
    "stdinLogCap": 4194304
  }
}
