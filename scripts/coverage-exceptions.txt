# Per-file coverage exceptions (plan-12).
#
# Files listed here are exempt from the per-file 95% gate because their
# uncovered remainder is genuinely not reachable from a unit test — live
# network I/O, socket/TTY syscalls, subprocess (linker/codesign) invocation, or
# GUI event loops — and is instead covered by the tests/ integration harness.
# cargo-llvm-cov 0.8.7 does not honor inline `// coverage:off` markers, so the
# exclusion is tracked here (auditable in review) rather than in-source.
#
# Format: one repo-relative path per line, optional `# reason` after the path.
# Blank lines and lines starting with # are ignored.
#
# This list is populated as Tier-C/D files are triaged; each entry must name the
# specific syscall/network/subprocess boundary that blocks unit coverage.

# --- Tier-D CLI / registry orchestration (covered by tests/repo_acceptance.rs) ---
src/cli/pkg.rs        # publish/add/transfer/check-abi drive a live HTTP registry + on-disk key mutation; pure arg parsing/validation is unit-covered
src/cli/resolve.rs    # resolve/update/install fetch the registry over HTTP; pure lock read/write/diff + version selection is unit-covered
src/cli/dispatch.rs   # ex-src/main.rs top-level router (bug-340 B1, c565abda8): run() reads std::env::args and every arm spawns the system linker via build_project, drives the live HTTP registry via run_pkg/run_repo/run_{machine,key,org,token}_command, or calls process::exit — none reachable in-process without terminating the test runner; is_help_flag/USAGE arms cannot be isolated from run()'s env::args entry
src/cli/build/signing.rs  # load_build_signing_info (signing.rs:47-158) calls mfb_repository::client::request_attestation against a live HTTP registry and reads the machine's registered ident-key files; its pure helpers (signing_ident/apply_signing_metadata/executable_signing_metadata_json) are unit-covered. bug-327 T2-10 (19a95d448) split this out of the now-deleted src/cli/build.rs, whose stale exception this replaces
src/cli/repo.rs       # register/auth/link/trust perform live HTTP + key-store mutation; Usage/arg validation is unit-covered
src/main.rs           # process entry point: fn main only forwards to cli::dispatch::run() (the excepted dispatch body moved to src/cli/dispatch.rs by bug-340 B1); fn main is never invoked by a unit test
src/target.rs         # write_executable/nir/plan/object dispatchers invoke per-OS backends that spawn linkers/codesign over a full IrProject; BuildTarget/NativeBuildMode parsing is unit-covered

# --- Required-for-exhaustiveness defensive match arms (unreachable at runtime) ---
src/syntaxcheck/resources.rs  # ~94.6%: `Type::Result` arms in exhaustive type-walkers (contains_thread/is_copyable/is_thread_sendable — Result isn't source-nameable there) and thread.send/transfer resource arms rejected earlier at declaration. The arms are required for match exhaustiveness so they cannot be deleted; every reachable rule IS unit-tested.

# --- plan-68: files capped below 95% by genuinely-unreachable defensive / integration arms (each backfill-audited against the fresh lcov; the coverable body IS unit-tested, the residual is proven unreachable) ---
repository/src/main.rs  # async server entrypoint main() (:48-284, wrapped in `// coverage:off` which cargo-llvm-cov 0.8.7 ignores): reads env::args, opens the on-disk SQLite store, constructs the blob store, binds the listen socket, runs server::serve(...).await, and process::exit. Every pure helper (the parse_* arg helpers, the store-op helpers, init_root_expires_at) is unit-covered; the entrypoint body is socket-bind + live-HTTP + argv/exit
src/cli/build/test_mode.rs  # make_temp_output_dir exclusive-create loop caps the file at ~86%: the AlreadyExists retry (:43) and the 64-retry terminal (:50-51) require a pid+nanos temp-path collision, and the non-AlreadyExists create failure (:44-46) requires a process-global std::env::temp_dir mutation racy under the parallel suite. run_test_binary + generate_coverage_report are unit-covered
src/cli/build/resources.rs  # copy_resources canonicalize().map_err arms (:77-88) cap the file at ~94%: walk_root has already passed is_dir() and project_root is a validated build root, so canonicalize() only fails on a TOCTOU race no unit test can force. All prefix/glob/dest-mapping/copy logic (incl. the create_dir_all + fs::copy error arms) is unit-covered
src/os/linux/appimage/squashfs/mod.rs  # SquashFS §4.9 defensive invariant guards cap the file at ~93%: compile-time-constant checks (BLOCK_SIZE==1<<BLOCK_LOG, COMPRESSION_ZLIB!=0, FLAGS&FLAG_COMPRESSOR_OPTIONS, :404-410), always-holds byte-accounting asserts (root-inode offset :383, table adjacency :415-435), and the >4-GiB extended-inode reject (:322-325) that needs a buffer no unit test can allocate. The tree-plan / inode / directory serializers are unit-covered (tests in the sibling squashfs/tests.rs)
src/cli/mod.rs  # capped at ~94.4%: dispatch_command_error is `-> !` / std::process::exit (:29-40, cannot run in-process without killing the test runner) and confirm's interactive branch (print+flush+stdin.read_line, :203-214) needs a real TTY — the harness stdin is non-interactive so confirm returns at the is_terminal() guard (:196) before reaching it; the stage_bytes mid-write sync-failure arm (:59-61) is likewise not deterministically forceable. All pure helpers (answer_is_yes/confirm-guard/local_paths_for_repo/stage_package_blob/commit_staged_package/install_verified_package/install_vendor_file) are unit-covered
src/binary_repr/mod.rs  # caps at ~94.6%: read_package_type_exports_resolved's 64-level re-export depth-cap Err (:600-605, needs 64 stacked .mfp packages) and the path.parent()==None arm (:612, only for a root-level `.mfp` with no packages dir) are both unreachable from a unit test. The owner-absent / owner-present / mixed foreign+non-foreign type resolution IS unit-covered (src/binary_repr/tests/cross_package_tests.rs)
src/os/linux/appimage/mod.rs  # caps at ~92.4%: the seal() blob-length supply-chain guard (:177-184) is unreachable with the embedded runtime (proven by the every_runtime_ends_exactly test), and the file's inline #[cfg(test)] module's enum-destructure assertion-failure arms (`let SquashNode::Dir{..} = .. else { panic!() }` at :463-524) are never taken on a green run. Every production path (read_appdir / read_dir_node / seal happy path) is unit-covered. Follow-up: moving the inline test module to a sibling appimage/tests.rs (the squashfs pattern) would drop the test arms from this file's denominator and let the whole-file exception be lifted
