' Benign base package for the PKG-07 security fixture. `generate.py`
' builds this, then rewrites the compiled `.mfp` into the malicious variant
' committed under tests/syntax/security/pkg-07-need-overflow/packages/.
EXPORT FUNC run() AS Integer
  RETURN 42
END FUNC
