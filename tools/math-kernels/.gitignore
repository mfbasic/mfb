# Built oracle binary (regenerated by capture.sh / the build line in the README).
capture_ref
capture_ref.dSYM/

# Captured reference vectors are scratch *here* — the committed copy lives at the
# in-tree test-data path the implementer passes to capture.sh. Regenerate with
# ./capture.sh on the reference macOS.
reference/

# Python / venv cruft.
__pycache__/
*.pyc
.venv/
venv/
