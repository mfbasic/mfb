# <localName>

<one-line description of what the function does>

## Synopsis

```
<module>::<localName>(<param> AS <Type>, ...) AS <ReturnType>
```

## Package

<name of the package this function belongs to>

## Imports

<the IMPORT statement a program needs, or a note that it is always available>

## Description

<what the function computes, how each argument is interpreted, boundary and
edge-case behavior, units, ordering, mutation/side effects, and platform notes>

## Overloads

**`<signature>`**

<what this overload does and how it differs from the others>

**`<signature>`**

<what this overload does and how it differs from the others>

## Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `<param>` | `<Type>` | <meaning, accepted range/values, units> |
| `<param>` | `<Type>` | <meaning, accepted range/values, units> |

## Return value

| Type | Description |
| --- | --- |
| `<ReturnType>` | <what is returned on success, including boundary results> |

## Errors

| Code | Name | Raised when |
| --- | --- | --- |
| `<code>` | `<ErrName>` | <condition> |
| `<code>` | `<ErrName>` | <condition> |

## Type checking

<the types this generic function accepts and any compile-time constraints>

## Examples

<short caption describing the example>:

```
<runnable mfb code>
```

## See also

- `mfb man <module> <relatedFunction>`
- `mfb man <module> <relatedFunction>`
